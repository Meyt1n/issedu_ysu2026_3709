# 移动端发布门禁证据索引

> 文档用途：为 MOB-148（#234）提供已提交文档、自动化检查和待补充人工证据的单一索引。
> 本索引不改变发布裁决；缺少外部证据时，发布结论仍必须是 `BLOCKED`。

## 1. 结论摘要

截至 2026-09-01 的本机复核证明 PWA/Capacitor 工程可以复跑，资源预算、弱设备模拟、隐私扫描、Android 静态安全检查和 debug APK 构建均通过，并在荣耀真机安装了提交 `3090b39` 的最新 APK，完成主要路由和视觉壳层烟测。它没有证明受控后端已联调、完整 Android 真机矩阵或回滚已签收，因此当前结论为：

| 结论 | 当前状态 | 原因 |
| --- | --- | --- |
| `RUNTIME_RELEASE` | `BLOCKED` | 缺受控后端、完整 Android 真机/无障碍/语音矩阵和回滚证据；当前只有 APK 安装启动烟测 |
| `FULL_VISION_PRODUCTION` | `BLOCKED` | 除运行时证据外，还缺固定数据集、视觉后端和生产视觉门禁 |

## 2. 已提交事实源

| 证据主题 | 文档/脚本 | 说明 |
| --- | --- | --- |
| 最终裁决规则 | [MOB-148-移动端最终发布门禁](MOB-148-移动端最终发布门禁.md) | 定义 `RUNTIME_RELEASE` 与 `FULL_VISION_PRODUCTION` 的独立门禁 |
| Android 包操作 | [Android 演示发布包操作记录](Android演示发布包操作记录.md) | 记录命令、产物边界和本机阻塞项 |
| 受控环境 | [MOB-147-受控发布环境运行手册](MOB-147-受控发布环境运行手册.md) | 记录合成数据、版本指纹、HTTPS、联调、升级和回滚要求 |
| 无障碍约束 | [无障碍模式设计说明](无障碍模式设计说明.md) | 定义触控目标、焦点、文字/图标/颜色和语音降级要求 |
| 性能场景 | `scripts/perf-budget.mjs` | 基线与低端设备模拟的冷启动、路由、离线恢复和资源预算 |
| 记录模板 | `mob148-final-release-record.template.json` | 受控发布证据的未提交记录格式 |
| 风险告警字段真机矩阵 | [MOB-173 Android 风险告警字段验收矩阵](../../docs/testing/MOB-173-Android风险告警字段验收矩阵.md)、[Android 演示局部记录](../../docs/testing/MOB-173-Android演示字段局部走查记录-20260831.md)、`scripts/verify-android-risk-evidence.mjs` | 演示真机已复现列表/详情字段与缺失字段安全降级；完整/缺失审计字段、合并/预算/严重级别和截图隐私仍需真实受控后端与设备持有人签收，不能视为发布通过 |
| 环境行动卡真实天气联调 | [MOB-163 Android/PWA 环境行动卡联调验收矩阵](../../docs/testing/MOB-163-Android-PWA环境行动卡联调验收矩阵.md)、`scripts/verify-android-environment-evidence.mjs` | 覆盖授权、城市编码/访问目的拒绝、超时/限速/上游失败、旧缓存与去重；只允许城市/区县编码，不得提交精确坐标；真实提供方与 Android/PWA 设备签收前保持 BLOCKED |
| 联机三步清单真机验收 | [MOB-164 Android/PWA 联机三步清单验收矩阵](../../docs/testing/MOB-164-Android-PWA联机三步清单验收矩阵.md)、`scripts/verify-android-onboarding-evidence.mjs` | 覆盖首次配置、地址拒绝、多家庭显式选择、连接失败、能力探测未完成、TalkBack/特大字号、重试锁定和隐私边界；设备持有人签收前不得视为通过 |
| 弱网列表真机验收 | [MOB-171 Android/PWA 弱网列表验收矩阵](../../docs/testing/MOB-171-Android-PWA弱网列表验收矩阵.md)、[Android/PWA 受控局部验收记录](../../docs/testing/MOB-171-Android-PWA受控局部验收记录-20260901.md)、[Android 真机局部走查记录](../../docs/testing/MOB-171-Android真机局部走查记录-20260831.md)、`scripts/mob171-weak-network-fixture.mjs`、`scripts/verify-android-weak-network-evidence.mjs` | 最新 Debug APK 已在荣耀真机完成骨架、连接超时、服务端慢/504、空集合、部分数据、减少动效和重试锁定受控复核，PWA 同夹具对照并留存脱敏附件；系统级断网恢复、TalkBack、特大字号及完整维护者签收仍缺，不能视为发布通过 |
| 计划提醒真机验收 | [MOB-172 Android 计划提醒验收矩阵](../../docs/testing/MOB-172-Android计划提醒验收矩阵.md)、[Android 真机局部走查记录](../../docs/testing/MOB-172-Android真机局部走查记录-20260831.md)、`scripts/verify-android-reminder-evidence.mjs` | 覆盖前台/后台/锁屏/息屏/划掉/重启、电池优化、自启动、勿扰模式和锁屏隐私；局部记录已核对私有通知渠道和默认隐私文案，设备持有人完整签收前不得视为通过 |
| 助手聊天气泡与时间语义 | `src/views/AssistantView.vue`、`src/utils/chatPresentation.ts`、`../shared/voice/chatSession.ts` 与对应单测 | 用户/助手气泡层级、连续消息分组、时间戳持久化、长文本换行和读屏「角色—内容—时间」语义；本机 Chromium 320×800 视口已验证无横向溢出，高对比与减少动效下气泡边框/过渡和时间标签正常；2026-08-31 荣耀 AAP-AN00 原生 APK 已完成演示提问、长文本/URL 换行、时间标签、屏幕边界，以及特大字号/高对比/减少动效组合验收；预装读屏服务激活态下系统确认已启用，聊天节点暴露完整角色/内容/时间语义，测试后服务和 App 偏好均已恢复。PWA 安装态与发布签收仍由 MOB-148 执行。 |
| 助手联网搜索与出网范围 | `src/views/AssistantView.vue`、`src/utils/networkSearch.ts` 与对应回归 | 演示/未探测/未开放时显示禁用开关和可朗读说明；仅服务端声明 `external-web` 且用户显式开启时出网；域名标注、仅本地知识重试、服务端降级 trace 和隐私边界；2026-08-31 荣耀 AAP-AN00 已验证演示模式和长辈模式下的禁用语义、出网范围说明及设备无语音时文字降级；真实 provider 的限速/超时/上游失败链路作为联机发布门禁证据 |
| 手机知识条目只读浏览 | `src/views/KnowledgeLibraryView.vue`、`src/views/KnowledgeDocumentView.vue`、`src/components/AssistantEvidencePanel.vue`、[MOB-162 验收记录](../../docs/testing/MOB-162-手机知识条目只读验收记录.md) | 已批准条目只读列表/详情、未批准正文过滤、引用分块定位、逐块/全部折叠展开与回到顶部；38 个测试文件 / 351 项回归、390×844 PWA 交互烟测和荣耀 AAP-AN00 入口/详情烟测完成；本 PR 增量 APK、受控 403、TalkBack/特大字号仍需发布门禁签收 |

## 3. 可复跑自动化

在 `APP/` 目录执行：

```powershell
npm run check
npm run test
npm run build
npm run android:sync
npm run privacy:scan
npm run audit:android-security
npm run audit:pwa
npm run perf:budget
npm run perf:budget:low-end
npm run test:perf-budget
npm run test:privacy-scan
npm run release:manifest -- --out release
```

本次复核结果为：`check`、测试（32 个文件 / 290 tests）、构建、Capacitor 同步、隐私、安全、PWA、基线性能、低端设备性能、两个回归测试和产物清单均通过。基线性能 JS gzip 为 159.29 KiB；低端模拟可交互 841 ms、路由最慢 133 ms、断网恢复 177 ms。`release/` 只用于本机临时产物，不提交仓库。

性能脚本会在每个新的浏览器 context 中先按真实用户路径确认首次隐私告知，再进入提醒、设置和断网恢复场景。这样既保留隐私告知语义，也避免自动化绕过真实 UI。

性能命令必须顺序执行：`perf:budget` 和 `perf:budget:low-end` 都会重建 `dist/`，并行执行会造成不同构建产物混杂，导致体积报告失真。

## 4. 2026-08-28—08-31 新增本机证据

- `scripts/build-apk.ps1` 使用 JDK 21 与已配置 Android SDK 成功生成 debug APK（约 4.54 MiB）；APK、签名材料和 `local.properties` 未提交。
- 荣耀 AAP-AN00（Android 16、Android System WebView 138.0.7204.179、1272×2800）完成 USB 安装、启动和助手演示提问；UIAutomator 证实用户/助手气泡的角色、内容、时间标签可读，长文本/URL 在屏幕内换行。提交 `9c31536` 的 APK 又完成特大字号/高对比/减少动效组合走查；助手末条及「朗读回答」可滚动到固定导航上方，系统确认预装读屏服务已激活，测试后读屏服务与 App 偏好均已恢复。Windows 端需要 `ADB_LIBUSB=1` 才能稳定枚举该设备。
- MOB-162 只读入口烟测：同一荣耀设备从「我的」进入「阅读助手用到的知识条目（只读）」，列表只显示已批准演示条目，打开详情可读到来源/许可/索引版本/生效时间和 3 个正文分块；未批准演示草稿未进入列表。分块折叠/展开和回到顶部已由 390×844 PWA 烟测覆盖；新 APK 尚待 Java 21 环境重建后复跑。
- 以上是构建/安装及 MOB-160 聊天气泡烟测，不包含完整升级、后台恢复、权限、TTS、触觉、小屏、PWA 安装态和回滚签收；设备序列号不写入仓库。

## 4.1 2026-09-01 综合真机走查补充

- [MOB-160～MOB-175 Android 真机综合走查记录](../../docs/testing/MOB-160-175-Android真机综合走查记录-20260901.md)：荣耀 AAP-AN00 已通过 16 个主要路由的演示加载、横屏根宽度/无横向滚动抽查、助手草稿旋转保持检查，并在 `3090b39` 最新 Debug APK 上复跑主要路由与视觉壳层；MOB-172 另有短延迟通知真实入栏及点击回应用 smoke。记录仍明确区分演示/局部证据与发布签收。
- 本次 `npm run test -- --reporter=dot`（39 文件 / 356 项）、`npm run check`、`npm run audit:responsive`、`npm run privacy:scan`、`npm run audit:android-security`、`npm run android:sync` 和 `assembleDebug` 均通过；最新安装包 APK SHA-256 为 `E6E563B070128B74697C099892FCE750C14BFDEFCB062254B9EA6F485F80B59A`。assembleDebug 使用本机 JDK 24 的未跟踪临时兼容设置，已恢复依赖文件原状。综合记录不改变各 Issue 的“待验收/进行中”状态，也不解除 `RUNTIME_RELEASE` 的 `BLOCKED`。
- [MOB-506 Android 私网 HTTP 真机验收记录](../../docs/testing/MOB-506-Android私网HTTP真机验收记录-20260901.md)：在荣耀 AAP-AN00 的 `https://localhost` 活动 WebView 中，受控局域网 HTTP fixture 的 `/health` 与能力探测均返回 200；Debug-only mixed-content 放行和 Release HTTPS-only 边界均已记录。该 smoke 解除 #506 的软件阻断证据，但不替代 #246 的视频上传端到端验收。
- [MOB-171 Android/PWA 受控局部验收记录](../../docs/testing/MOB-171-Android-PWA受控局部验收记录-20260901.md)：最新 Debug APK 与 Chromium 在同一合成弱网夹具上完成骨架、超时、504、空集合、部分数据保留、减少动效和重试锁定复核；记录明确列出系统级断网恢复、TalkBack、特大字号和维护者签收的剩余门禁。

## 5. 待补证据

MOB-161 原生 APK 入口烟测（2026-08-31）：荣耀 AAP-AN00 上默认关闭且禁用「允许本次脱敏联网参考」，禁用原因可读；展开「出网范围说明」后可读到演示模式不出网、会/不会出网边界、默认关闭和人工确认提示。该证据不等同于真实联网搜索或 TalkBack 签收。

以下项目不能由本机静态检查代替：

1. 启动受控 HTTPS 后端和合成数据，运行 `npm run verify:linkage`，记录 APP/服务端提交、API 版本和种子指纹。
2. 维护者在最终候选提交上重建 APK，记录 APK SHA-256、版本和工具链；APK 与签名材料不得提交（本机 debug 构建已完成，但尚未成为受控发布签收）。
3. 在 Android 真机执行升级、切后台、断网/恢复、权限、TalkBack、TTS、触觉和 320/375px 小屏验收，并填写对应证据模板。
4. 安装失败或候选版本撤回时，演练恢复上一份已验证的 PWA shell、APK、API 和合成数据指纹。
5. 由维护者把脱敏记录填入未跟踪的 `mob148-final-release-record.json`，再对 #234 做最终裁决。

## 6. 维护规则

- 只记录可定位的命令、文件、日期、提交哈希和脱敏证据路径。
- 不把 Mock、浏览器模拟或 Issue 已关闭写成真机/生产通过。
- 新证据应追加到对应事实源，并同步 `docs/vibe-coding/12-需求追踪矩阵.md`。
- 任何证据过期、版本不一致或回滚指纹缺失，都应恢复为 `BLOCKED`。
