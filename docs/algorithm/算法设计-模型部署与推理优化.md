# 算法设计-模型部署与推理优化

> 本文档是家健镜系统机器学习模型部署与推理优化的完整设计说明，覆盖模型导出、推理优化、服务部署、性能监控。

## 1. 概述

### 1.1 部署目标

1. 低延迟：推理时间 < 100ms
2. 高吞吐：支持并发推理
3. 资源高效：GPU/CPU 利用率高
4. 易于扩展：支持模型热更新
5. 可观测：推理性能可监控

### 1.2 部署方式

| 方式 | 优点 | 缺点 | 适用场景 |
| --- | --- | --- | --- |
| REST API | 简单、通用 | 延迟较高 | 低并发 |
| gRPC | 高性能、流式 | 复杂 | 高并发 |
| 边缘部署 | 低延迟、隐私 | 资源有限 | 移动端 |
| 批处理 | 高吞吐 | 延迟高 | 离线任务 |

## 2. 模型导出

### 2.1 ONNX 导出

```python
import torch
import torchvision.models as models

def export_to_onnx(model, input_shape, output_path):
    model.eval()
    dummy_input = torch.randn(*input_shape)

    torch.onnx.export(
        model,
        dummy_input,
        output_path,
        input_names=["input"],
        output_names=["output"],
        dynamic_axes={
            "input": {0: "batch_size"},
            "output": {0: "batch_size"},
        },
        opset_version=11,
    )
    print(f"模型已导出到 {output_path}")

# 使用示例
model = models.resnet50(pretrained=True)
export_to_onnx(model, (1, 3, 224, 224), "resnet50.onnx")
```

### 2.2 TorchScript 导出

```python
import torch

def export_to_torchscript(model, input_shape, output_path):
    model.eval()
    dummy_input = torch.randn(*input_shape)

    # 追踪方式
    traced_model = torch.jit.trace(model, dummy_input)
    traced_model.save(output_path)

    # 脚本方式（支持控制流）
    # scripted_model = torch.jit.script(model)
    # scripted_model.save(output_path)
```

### 2.3 TensorFlow SavedModel

```python
import tensorflow as tf

def export_to_savedmodel(model, output_path):
    tf.saved_model.save(model, output_path)

    # 转换为 TFLite
    converter = tf.lite.TFLiteConverter.from_saved_model(output_path)
    tflite_model = converter.convert()
    with open("model.tflite", "wb") as f:
        f.write(tflite_model)
```

## 3. 推理优化

### 3.1 模型量化

```python
import torch
from torch.quantization import quantize_dynamic

def quantize_model(model):
    # 动态量化（权重 int8，激活 float）
    quantized_model = quantize_dynamic(
        model,
        {torch.nn.Linear, torch.nn.Conv2d},
        dtype=torch.qint8,
    )
    return quantized_model

# ONNX 量化
from onnxruntime.quantization import quantize_dynamic, QuantType

def quantize_onnx(input_path, output_path):
    quantize_dynamic(
        input_path,
        output_path,
        weight_type=QuantType.QUInt8,
    )
```

### 3.2 模型剪枝

```python
import torch
import torch.nn as nn

def prune_model(model, amount=0.3):
    # 全局非结构化剪枝
    parameters_to_prune = []
    for name, module in model.named_modules():
        if isinstance(module, nn.Conv2d):
            parameters_to_prune.append((module, "weight"))

    torch.nn.utils.prune.global_unstructured(
        parameters_to_prune,
        pruning_method=torch.nn.utils.prune.L1Unstructured,
        amount=amount,
    )

    # 移除剪枝重参数化
    for module, _ in parameters_to_prune:
        torch.nn.utils.prune.remove(module, "weight")

    return model
```

### 3.3 知识蒸馏

```python
import torch.nn as nn
import torch.nn.functional as F

class KnowledgeDistillation:
    def __init__(self, teacher_model, student_model, temperature=4.0, alpha=0.7):
        self.teacher = teacher_model
        self.student = student_model
        self.temperature = temperature
        self.alpha = alpha

    def train_step(self, inputs, labels, optimizer):
        self.teacher.eval()
        self.student.train()

        with torch.no_grad():
            teacher_outputs = self.teacher(inputs)

        student_outputs = self.student(inputs)

        # 蒸馏损失
        distillation_loss = F.kl_div(
            F.log_softmax(student_outputs / self.temperature, dim=1),
            F.softmax(teacher_outputs / self.temperature, dim=1),
            reduction="batchmean",
        ) * (self.temperature ** 2)

        # 学生损失
        student_loss = F.cross_entropy(student_outputs, labels)

        # 总损失
        loss = self.alpha * distillation_loss + (1 - self.alpha) * student_loss

        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

        return loss.item()
```

### 3.4 批处理优化

```python
class BatchInference:
    def __init__(self, model, max_batch_size=32, max_wait_time=0.01):
        self.model = model
        self.max_batch_size = max_batch_size
        self.max_wait_time = max_wait_time
        self.queue = []

    async def predict(self, input_data):
        # 动态批处理
        future = asyncio.Future()
        self.queue.append((input_data, future))

        if len(self.queue) >= self.max_batch_size:
            await self._process_batch()

        return await future

    async def _process_batch(self):
        if not self.queue:
            return

        batch = self.queue[:self.max_batch_size]
        self.queue = self.queue[self.max_batch_size:]

        inputs = torch.cat([item[0] for item in batch])
        with torch.no_grad():
            outputs = self.model(inputs)

        for i, (_, future) in enumerate(batch):
            future.set_result(outputs[i])
```

## 4. 推理服务

### 4.1 FastAPI 服务

```python
from fastapi import FastAPI
from pydantic import BaseModel
import torch
import numpy as np

app = FastAPI()
model = None

class PredictRequest(BaseModel):
    data: list

class PredictResponse(BaseModel):
    predictions: list

@app.on_event("startup")
async def load_model():
    global model
    model = torch.jit.load("model.pt")
    model.eval()

@app.post("/predict", response_model=PredictResponse)
async def predict(request: PredictRequest):
    input_tensor = torch.tensor(request.data)
    with torch.no_grad():
        output = model(input_tensor)
    return PredictResponse(predictions=output.tolist())
```

### 4.2 ONNX Runtime

```python
import onnxruntime as ort

class ONNXInference:
    def __init__(self, model_path):
        self.session = ort.InferenceSession(
            model_path,
            providers=["CUDAExecutionProvider", "CPUExecutionProvider"],
        )
        self.input_name = self.session.get_inputs()[0].name

    def predict(self, input_data):
        outputs = self.session.run(
            None,
            {self.input_name: input_data},
        )
        return outputs[0]
```

### 4.3 Triton Inference Server

```python
# Triton 模型仓库结构
# model_repository/
# └── resnet50/
#     ├── config.pbtxt
#     └── 1/
#         └── model.onnx

# config.pbtxt
name: "resnet50"
platform: "onnxruntime_onnx"
max_batch_size: 32
input [
  {
    name: "input"
    data_type: TYPE_FP32
    dims: [3, 224, 224]
  }
]
output [
  {
    name: "output"
    data_type: TYPE_FP32
    dims: [1000]
  }
]
instance_group [
  {
    kind: KIND_GPU
    count: 1
  }
]
```

## 5. 性能监控

### 5.1 推理指标

```python
class InferenceMetrics:
    def __init__(self):
        self.latency_histogram = Histogram("inference_latency_seconds", ["model"])
        self.throughput_counter = Counter("inference_requests_total", ["model"])
        self.batch_size_gauge = Gauge("inference_batch_size", ["model"])

    def record(self, model_name, latency, batch_size):
        self.latency_histogram.labels(model_name).observe(latency)
        self.throughput_counter.labels(model_name).inc()
        self.batch_size_gauge.labels(model_name).set(batch_size)
```

### 5.2 模型性能基准

| 模型 | 输入尺寸 | CPU 延迟 | GPU 延迟 | 内存占用 |
| --- | --- | --- | --- | --- |
| ResNet50 | 224x224 | 50ms | 5ms | 100MB |
| YOLOv8n | 640x640 | 100ms | 10ms | 50MB |
| BERT-base | 128 tokens | 80ms | 8ms | 400MB |

## 6. 模型部署检查清单

- [ ] 模型导出（ONNX/TorchScript）
- [ ] 模型量化
- [ ] 模型剪枝
- [ ] 知识蒸馏
- [ ] 批处理优化
- [ ] 推理服务
- [ ] GPU 加速
- [ ] 性能监控
- [ ] 模型版本管理
- [ ] A/B 测试
- [ ] 回滚机制
- [ ] 资源限制

---

*模型部署是 AI 应用的最后一公里。高效的推理优化，让模型在生产环境中快速可靠运行。*
