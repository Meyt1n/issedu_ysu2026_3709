# HCT-201 固定药品集阻塞审计（2026-08-24）

## 结论

本次审计完成了候选盘点和 OCR 初筛，但 HCT-201 仍然是 `BLOCKED`，不能把当前数据改成 `APPROVED`，不能启动基于该集的正式发布训练。

当前已确认：

- 家庭可信域外存在 `canonical/v1.2-annotation-reviewed-candidate`，共 1,453 条 manifest 记录；
- 1,453 条记录状态均为 `QUARANTINED`，正式 split 均为 `quarantine`；
- 本次从已有 test 候选中初筛出 20 个不同的药品文字候选；这只是 OCR 线索，不是药品主数据确认；
- 已找到 2 条非纸盒 hard-negative，可作为 unknown 待复核入口；
- 当前可核验的 conflict 样本为 0；
- 当前没有完整的 `authorization_evidence_ref`、真实分组证据、R3 `review_record_ref`、固定集审批引用和删除传播演练证据。

因此，阻塞原因已经从“完全没有候选”细化为：**候选数量足够，但批准证据和固定集结构尚未完成**。

## 与门禁的对应关系

| 门禁 | 本次事实 | 当前结果 | 需要的解除证据 |
|---|---|---|---|
| 12～20 个批准真实药品 | 20 个 OCR 药品候选，均未确认 `drug_id` | BLOCKED | 药盒人工复核、主数据/条码核对、稳定 drug_id、R3 复核 |
| train/validation/test 真实分组 | 现有 group 为 `proxy-phash2-*`，session 为 `roboflow-v2-unknown` | BLOCKED | 真实包装实体、采集会话、设备/日期和 grouping evidence |
| fixed test | 20 个候选均继承 `quarantine`，不是 `split=test` | BLOCKED | 先冻结 test，登记分布和 manifest SHA-256，禁止调参污染 |
| unknown | 2 条 hard-negative 仍 QUARANTINED | BLOCKED | 逐图非目标确认、许可/隐私、unknown_reason、R3 复核 |
| conflict | 没有可核验矛盾证据 | BLOCKED | OCR/条码/主数据/人工修正中至少两条可定位冲突证据 |
| 授权和删除 | 页面 CC BY 4.0 不能替代项目用途、隐私和撤销传播证据 | BLOCKED | 许可快照、署名/修改说明、训练用途确认、删除联系人和演练 |

## 已完成的仓库工作

- 新增[固定药品集候选清单](HCT-201-固定药品集候选清单-20260824.md)，记录 20 个 OCR 初筛候选、2 个 unknown 候选以及 conflict 缺口；
- 没有把任何图片、模型权重或未经批准的 manifest 提交到仓库；
- 没有修改原始候选数据、删除用户目录文件或改变现有 `QUARANTINED` 状态；
- 保留已有 `scripts/hct201_formal_gate.py` 与 `scripts/hct201_fixed_set_gate.py`，批准版生成后可直接复用。

## 不应采取的“解决方式”

- 不把 `QUARANTINED` 批量替换成 `APPROVED`；
- 不把 OCR 暂定药名当作药品身份真值；
- 不把 pHash、文件名或 Roboflow 的原始 train 划分当作真实实体/会话分组；
- 不把两条 hard-negative 直接改成 unknown，也不凭猜测制造 conflict；
- 不把用户口头“合规”说明写成不存在的作者授权或 R3 审核记录。

## 下一步最短闭环

如果要真正解除当前表格中的功能阻塞，需要用户/数据作者补交一个受控授权包，而不是继续改代码。授权包至少包含：

1. 来源版本页、许可快照、署名要求和修改声明；
2. 训练用途确认和撤销/删除联系人；
3. 22 条候选的人工药名/边界/隐私复核表；
4. 每个样本所属真实包装实体和采集会话；
5. 至少一条可复核的 conflict 双证据记录；
6. `Shen-huang-123` 的 R3 复核记录。

拿到这些材料后，才能生成批准版 JSONL，再运行 formal gate 和 fixed-set gate。当前文档已经把候选和缺口固定下来，后续不需要重新扫描全部 1,453 张图片。
