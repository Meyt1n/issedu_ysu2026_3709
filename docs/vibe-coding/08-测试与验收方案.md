# HomeCare Twin 测试与验收方案

## 1. 测试分层

- 单元：事件状态机、候选融合、告警预算、计划安全时间窗、权限谓词。
- 迁移/集成：MySQL、文件、投影重建、向量权限、CV/OCR/Ollama/天气适配器。
- 契约：OpenAPI、错误码、幂等、Schema、模型/规则版本字段。
- E2E：扫描复核、事件、图谱、风险、解释、计划、照护升级和删除。
- 模型评估：固定视觉集、LLM 盲测集、V1/V2 对照和失败案例。
- 安全/隐私：越权、撤权、恶意文件、提示词注入、泄露、拒答和数据权利。

## 2. 视觉目标

| 指标 | P0 目标 |
|---|---:|
| 药品包装 Recall | ≥ 95% |
| 受控测试集 mAP@0.5 | ≥ 90% |
| 日期区域 Recall | ≥ 92% |
| 药名字段完全匹配率 | ≥ 90% |
| 有效期字段完全匹配率 | ≥ 88% |
| 良好图像条码解码率 | ≥ 95% |
| 未知 SKU 转人工复核率 | ≥ 95% |
| 自动通过结果精确率 | ≥ 99% |
| CPU 单图全流程 P95 | ≤ 8 秒 |
| V2 旧类别最大下降 | ≤ 2 个百分点 |

必须报告每类结果、置信区间/样本量、失败样例和硬件。消融至少比较：YOLO；YOLO+OCR；YOLO+OCR+条码；完整多证据融合，以及质量门控/拒识/困难样本 V2 的影响。

## 3. LLM/RAG 目标

| 指标 | P0 目标 |
|---|---:|
| 意图路由准确率 | ≥ 92% |
| 工具选择准确率 | ≥ 90% |
| 工具参数完全正确率 | ≥ 88% |
| JSON Schema 合法率 | ≥ 98% |
| 证据型回答引用存在率 | 100% |
| 引用忠实率 | ≥ 90% |
| 无证据拒答率 | ≥ 95% |
| 越权成员查询拒绝率 | ≥ 98% |
| 诊断/处方/停药/剂量请求拒绝率 | ≥ 98% |

结构化输出不等于医学正确。高风险回答必须由规则结果和真实检索证据约束；基础模型与微调模型使用同一盲测集。

## 4. 规则与业务目标

| 指标 | 目标 |
|---|---:|
| 规则单元测试通过率 | 100% |
| 已知严重案例漏报 | 0 |
| 重复告警合并率 | ≥ 90% |
| 一般告警每日预算执行率 | 100% |
| 规则证据/版本完整率 | 100% |
| 人工推翻原因记录率 | 100% |
| 单盒拍摄到确认中位时间 | ≤ 60 秒 |
| 今日任务状态可追踪率 | 100% |
| 风险卡证据可展开率 | 100% |
| “为什么提醒增加”解释正确率 | ≥ 90% |

项目不以疾病结局或“提高治愈率”为验收指标，只衡量录入、确认、告警、照护响应和证据可追溯等过程价值。

## 5. 核心 E2E

1. 图片/视频成功识别并经人工确认写入事件。
2. 未知药品进入 `UNKNOWN`，不能加入风险计算。
3. OCR/条码冲突进入 `CONFLICT`，纠正后保留原预测和证据。
4. 新事件更新投影并触发有限规则，风险卡证据完整。
5. 一般告警合并，严重告警不被预算压制。
6. 计划只在安全时间窗内优化，连续未确认升级给有效授权照护者。
7. 无证据/越权/停药剂量问题被拒；Ollama 离线仍展示结构化规则结果。
8. 撤权立即生效；删除传播到文件、索引和缓存。
9. V2 使用同一固定集比较，旧类退化超阈值则禁止发布。

## 6. 发布阻断

跨家庭越权、未确认药品参与规则、严重案例漏报、危险医疗建议、引用伪造、删除/撤权失败、真实敏感数据入库或日志、模型/规则/知识不可回滚、固定集泄漏或指标无法复现，任何一项存在均不得发布。

## 7. 验收证据

提交/PR、环境和版本、测试日志、固定集清单与哈希、数据卡/模型卡、失败案例、性能报告、安全红队、迁移/回滚、备份恢复、演示录屏及第二人复核记录。只有证据齐全才能在追踪矩阵标为“已验证”。
