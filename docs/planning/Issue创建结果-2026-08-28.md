# Issue 创建结果（2026-08-28）

## 执行结论

**Cloud Agent 未能直接创建 GitHub Issue** — 当前集成 token 对 `issues` API 返回 `403 Resource not accessible by integration`（与历史记录一致）。

## 维护者操作（一次性）

在具备 `issues:write` 权限的环境执行：

```bash
cd /path/to/issedu_ysu2026_3709
gh auth login   # 或 export GH_TOKEN=<PAT with issues:write>
bash scripts/create_story_backlog_issues.sh
```

脚本将创建 **42 个 Issue** 并写入本目录下的 `Issue创建结果-<日期>.md`：

| 批次 | 数量 | 范围 |
|---|---|---|
| §一 优先补票 | 15 | HCT-444～456 + 顺延 HCT-460～463 |
| §十一 下一迭代手机端 | 17 | MOB-159～175 |
| §十二 收口波次 | 10 | HCT-501～508、NEXT-M1/M2 |

## 创建后待办

1. 将 Issue 链接回填 `docs/vibe-coding/12-需求追踪矩阵.md` 对应行。  
2. 为 MOB-159～175 在 `docs/stories/` 新建正式 Story 文件（含验收/回滚）。  
3. 按 [编号顺延映射表](编号顺延映射表-2026-08-28.md) 重命名或新建撞号 Story 文件（可选，与矩阵同步）。

## 编号决策（已确认）

- 撞号 Story **顺延新号**，从 **HCT-460** 起。  
- **MOB-159～175** 全部纳入下一迭代。  
- `MOB-150` Android 安全撞号 → **MOB-176**。
