# MOB-171 手机弱网下列表骨架屏与超时重试文案

| 字段 | 内容 |
|---|---|
| Issue | 待开（`bash scripts/create_story_backlog_issues.sh` 后回填） |
| FR/NFR | NFR-04、NFR-07 |
| 状态 | 未开始（下一迭代规划 Story） |
| 关联 | MOB-112、MOB-113 |
| 风险 | R1 |

## 用户价值

弱网/超时时骨架屏与可操作重试，不误报为断网。。

## 范围

- 弱网/超时时骨架屏与可操作重试，不误报为断网。

## 非目标

- 不做诊断/处方/停药/换药/剂量判断；不买药/问诊/广告导流。
- 健康数据默认不出网（联网搜索遵循 ADR-0007 显式 opt-in）。

## 验收（Given / When / Then）

1. Given 联机且后端可用，When 进入相关页面，Then 功能按范围描述可用且有明确空态/降级文案。
2. Given 未授权或后端不可用，When 用户操作，Then fail-closed，不伪造成功。
3. Given 自动测试与 `npm run check/test/build`（APP），When CI 运行，Then 相关用例通过。

## 回滚

- revert 本 Story 对应 PR；无迁移时回滚为纯前端/配置撤销。
