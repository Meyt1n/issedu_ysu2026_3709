# MOB-171 手机弱网下列表骨架屏与超时重试文案

| 字段 | 内容 |
|---|---|
| Issue | [#526](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/526) |
| FR/NFR | FR-01 身份、家庭与成员级授权、NFR-04 基础档降级可用、NFR-07 可访问与可理解 |
| 状态 | 待验收（实现、自动回归和 Android/PWA 受控局部验收已完成；系统级断网恢复、TalkBack、特大字号和维护者签收待执行） |
| 关联 | MOB-112（[#170](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/170) API 错误展示统一）、MOB-113（[#171](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/171) 请求防重复与刷新策略） |
| 风险 | R2（加载态与错误态混淆会让用户反复重试或误判数据为空） |
| 负责人 | Shen-huang-123 |
| 复核人 | 维护者合并时完成最终复核 |
| 允许修改 | `APP/src/components/` 骨架屏组件、各列表视图、`APP/src/api/errors.ts` 文案、测试、本 Story、需求追踪矩阵 |

## 用户价值

MOB-112 统一了错误展示、MOB-113 定了防重复与刷新策略，但弱网下的中间态仍很粗糙：列表在加载时是空白，超时后的文案与「本来就没有数据」看起来一样。补骨架屏与分层的超时/重试文案，让「在加载」「加载失败」「确实为空」三种状态互不混淆。

## 范围

- 今日任务、成员、风险、时间线等主要列表的骨架屏（尊重减少动效设置）。
- 三态区分：加载中、失败可重试、空数据，文案与图示互不相同。
- 超时分层文案：连接超时、服务端慢、部分数据失败，分别给出可执行动作。
- 重试的节流与幂等，复用 MOB-113 的防重复策略，不新造机制。

## 非目标

- 不改错误码到文案的映射规则（属 MOB-112）。
- 不改请求防重复与刷新时机（属 MOB-113）。
- 不做离线写队列或本地乐观更新。
- 不做诊断/处方/停药/换药/剂量判断；不买药/问诊/广告导流。
- 健康数据默认不出网（联网搜索遵循 ADR-0007 显式 opt-in）。

## 验收（Given / When / Then）

1. Given 弱网首次加载列表，When 请求未返回，Then 展示骨架屏而非空白或「暂无数据」。
2. Given 减少动效开启，When 展示骨架屏，Then 不播放闪烁/位移动画，改用静态占位。
3. Given 请求超时，When 展示错误，Then 文案区分连接超时与服务端处理慢，并给出对应动作。
4. Given 服务端返回空集合，When 展示，Then 明确显示「确实没有数据」，与失败态视觉和文案都不同。
5. Given 连续点击重试，When 上一次请求仍在进行，Then 不发起重复请求。
6. Given TalkBack，When 状态在加载→失败→成功之间变化，Then 每次变化都有可朗读的状态播报。

## 安全、隐私与回滚

- 骨架屏只是占位，不得展示任何伪造的示例数据。
- 错误文案不得透露服务端内部实现或其它家庭是否存在。
- 回滚：移除骨架屏组件回到既有加载态，错误文案分层可独立保留。

## 实现与验证证据

- 新增 `ListLoadingState.vue` 统一列表加载态：骨架卡片只作为布局占位，使用 `role=status`、`aria-busy` 和可读状态文字；`SkeletonCard.vue` 在减少动效设置或系统减少动效时关闭扫光动画。
- `FamilyView.vue`、`AlertsView.vue`、`TodayView.vue`、`ScanView.vue`、`MeView.vue`、`MemberDetailView.vue` 和 `AlertDetailView.vue` 接入统一加载态；新增 `ListStatusAnnouncer.vue` 在加载完成后播报成功/空列表状态。
- `ErrorNotice.vue` 增加列表刷新期间的忙碌锁定、部分数据的警示样式和状态标题，避免连续点击重试；各列表加载入口增加请求中守卫。
- `presentListApiError()` 在不改变 MOB-112 通用映射的前提下补充列表上下文：连接等待超时、服务端 504/慢处理、部分数据保留分别给出下一步动作。
- 今日页使用 `Promise.allSettled` 保留已成功的任务/风险数据，趋势失败时单独提示并支持“重试补齐”；提醒页在风险列表成功但汇总失败时同样保留列表。
- `cd APP && npm run check` 通过；`npm run test` 通过（39 个测试文件、356 项）；`npm run build` 通过；`git diff --check` 通过。

## 真机验收交付

- 新增 [MOB-171 Android/PWA 弱网列表验收矩阵](../testing/MOB-171-Android-PWA弱网列表验收矩阵.md)，覆盖骨架屏、减少动效、连接超时、服务端慢/504、空集合、部分数据失败、重试锁定、断网恢复、TalkBack、窄屏特大字号和隐私占位共 12 条路径。
- 新增 `APP/scripts/verify-android-weak-network-evidence.mjs` 与 `npm run verify:weak-network-evidence`，要求设备、WebView、APK、PWA、API/错误契约、弱网注入和辅助设置元数据齐全，所有用例通过且附件哈希可复核；空模板按设计失败，不能以模拟器或普通浏览器代替真机签收。
- 新增 `APP/scripts/mob171-weak-network-fixture.mjs` 与 `npm run serve:mob171-fixture`，只生成虚构家庭/成员/任务/风险并可切换 `loading`、`timeout`、`slow`、`empty`、`partial` 场景；`partial` 通过时间线请求的 `context=weekly-trend` 只失败趋势请求，保证“任务/风险保留、趋势单独重试”可稳定复现。该夹具仅用于可信局域网验收，不得作为生产后端或真实数据源。
- 今日页为上下文自动选定增加刷新排队：首轮成员列表请求改变家庭/成员指纹时，后续快照不会被丢弃；错误/部分错误重试期间保留错误卡并锁定按钮，展示“正在重试…”，请求完成后再清理旧错误。
- 本次不改变 MOB-112 的错误码映射或 MOB-113 的请求去重机制，只补齐 MOB-171 的验收事实源和发布门禁。

### 2026-08-31 Android 局部复核

- [荣耀 AAP-AN00 真机局部走查记录](../testing/MOB-171-Android真机局部走查记录-20260831.md)记录了演示模式下从「家人」切换「提醒」时的骨架屏、`role=status`、`aria-busy`、成功状态播报和无伪造健康内容观察。
- 同次走查发现旧 APK 的应用内“减少动效”开关无法命中 `SkeletonCard.vue` 的 `scoped` 根节点选择器；已在当前分支改用完整 `:global(...)` 选择器，构建产物可确认 `html[data-motion=reduced] .skeleton-card:after` 静态隐藏规则。
- 修复后的 APK 尚未重建安装（本机缺少脚本要求的 JDK 21–24），因此该记录只作为局部事实，不能替代完整弱网/PWA/TalkBack 签收；Story 仍保持“待验收”。

### 2026-09-01 Android/PWA 受控局部复核

- [受控局部验收记录](../testing/MOB-171-Android-PWA受控局部验收记录-20260901.md)记录了最新 Debug APK（SHA-256 可复核）在荣耀 AAP-AN00 上的骨架、连接超时、服务端慢/504、空集合、部分数据、减少动效和重试锁定路径，以及同一合成夹具下的 PWA 对照结果。
- 受控复核确认：自动选定成员时刷新会排队；趋势失败不会清空今日任务和风险；错误卡在重试期间保持可见并切换为 disabled/“正在重试…”；合成数据请求不包含真实身份或健康正文。
- 系统级断网→恢复、TalkBack 逐次播报和特大字号仍未由设备持有人签收，本 Story 和 Issue 不能据此改为“已验证”。

### 本次可复跑命令

```powershell
cd APP
npm run verify:weak-network-evidence -- ../docs/testing/MOB-171-Android-PWA弱网列表验收矩阵.md
npm run check
npm run test
npm run build
npm run privacy:scan
```

## 未完成证据

- Android/PWA 系统级断网→恢复的完整人工矩阵、Android TalkBack 状态播报、特大字号窄屏复核、最终候选版本签名和维护者发布签收仍需执行；在此之前 Issue 保持打开。
- 连接超时、服务端慢、部分数据失败和断网恢复需要受控网络注入与请求计数证据，不能用本机演示数据或单次启动烟测替代。
