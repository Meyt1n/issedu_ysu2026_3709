# MOB-158：多家庭选择、隔离与切换状态清理

- Issue：[#243](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/243)
- 需求：FR-01、NFR-01、NFR-03、NFR-07
- 状态：进行中（选择、隔离、切换清理与自动回归完成；正式后端多家庭联调与 Android/PWA 真机验收待执行）
- 负责人：Shen-huang-123
- 复核人：维护者合并时完成最终复核
- 风险：R3（选错家庭会让照护者看到不该看到的成员与健康事件）
- 依赖：[MOB-133](MOB-133-正式鉴权与会话生命周期联调.md)（[#219](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/219)）、家庭列表与授权契约
- 允许修改：`APP/src/stores/session.ts`、`APP/src/data/index.ts`、`APP/src/data/httpProvider.ts`、家庭/设置/各页面切换逻辑、测试、本 Story、`APP/README.md`、需求追踪矩阵

## 用户价值

一个身份被授权访问多个家庭时，用户自己决定看哪个家庭，而不是被应用悄悄替他选。切换家庭会把上一个家庭的成员、任务、风险、时间线和上传草稿全部丢掉再重新加载；当前家庭被撤权或删除时回到"请重新选择"，不会自动跳到另一个家庭，也不会继续显示旧数据。

## 修掉的问题

`HttpDataProvider.resolveHouseholdId()` 原本固定取 `listHouseholds()[0]`。这有两个后果：用户完全无法选择家庭；而且**服务端列表顺序一变，同一个人打开应用就会看到另一个家庭的数据**，界面上却没有任何提示。

顺带修掉我在 MOB-133 里留下的同类捷径：`MeView` 为了给二次确认取 `household_id`，也是自己调 `listHouseholds()` 拿第一个。现在两处都走同一套"已选家庭"规则。

## 选择规则

| 情形 | 行为 |
|---|---|
| 已选家庭仍在服务端返回的列表里 | 使用它，与列表顺序无关 |
| 恰好一个可访问家庭 | 自动选定（保持低步骤体验），界面仍显示当前来源与"出现多个家庭时会要求选择" |
| 可访问多个但未选择 | **fail-closed**：`409 HOUSEHOLD_NOT_SELECTED`，不发出任何成员/事件请求 |
| 已选家庭不在列表里（撤权、删除、授权变化） | `404 HOUSEHOLD_UNAVAILABLE`，清除选择回到安全选择态，不自动换一个 |
| 一个家庭都没有 | 沿用既有 `404 NO_HOUSEHOLD` |

`HOUSEHOLD_UNAVAILABLE` 在 Provider 守卫里单独处理：只丢弃 Provider 缓存并清除家庭选择，**不**把整个授权边界判为"需重新验证"——那会连带清掉能力探测、要求用户重新测试连接，对"换个家庭"这件事过重。

## 切换时清理什么

`selectHousehold()` 经 `updateSession` 写入，`currentHouseholdId` 已加入 `SCOPE_FIELDS`，因此写入即触发 `resetSessionScopedState()` 的 `context` 级清理：

- 丢弃 `HttpDataProvider` 实例 —— 成员缓存、任务缓存、已解析家庭 ID、文件上传 Promise 与视觉任务草稿都挂在实例上，丢实例即清空；
- `currentMemberId` 一并清空，避免新家庭沿用旧成员 ID；
- `currentHouseholdId` 参与 `sessionContextKey`，各页面（如拍药盒的成员加载）据此判断本轮结果已过期并放弃写入；
- 能力快照在切换时清除后**立即重探**，而不是留一个空状态让相关入口全部禁用。

## Given / When / Then

- Given 当前正式会话可访问多个家庭；When 打开"我的 → 数据来源"；Then 展示服务端授权的家庭名称与当前选择，并要求显式选择，不默认取数组第一个。
- Given 用户切换家庭；When 确认切换；Then 旧家庭的成员、任务、风险、时间线、上传草稿、能力快照与页面缓存全部丢弃后再加载新家庭。
- Given 当前家庭被撤权、删除或列表顺序改变；When 刷新；Then 选择失效并回到安全选择态，不自动跳到另一个家庭，也不展示旧数据。
- Given 只可访问一个家庭；When 使用 APP；Then 自动选定保持低步骤，但仍显示当前来源与可切换条件。
- Given 未登录、无家庭、无权家庭或请求失败；When 打开选择器；Then 显示可恢复错误，文案不透露隐藏家庭是否存在。

## 实现证据

- `APP/src/stores/session.ts`：新增 `currentHouseholdId`（仅最小标识），纳入 `SCOPE_FIELDS` 与 `sessionContextKey`，`normalizeSession` 一并规范化；
- `APP/src/data/httpProvider.ts`：`SessionContext` 增加 `householdId`；`resolveHouseholdId` 按上表判定；新增 `listHouseholds()` 只返回 ID 与名称；
- `APP/src/data/demoProvider.ts`：演示模式返回单个虚构家庭，让选择界面对两种模式一致；
- `APP/src/data/types.ts`：`HouseholdOption` 与 `DataProvider.listHouseholds`；
- `APP/src/data/index.ts`：`selectHousehold` / `clearHouseholdSelection`，以及守卫里对 `HOUSEHOLD_UNAVAILABLE` 的单独处理；
- `APP/src/api/errors.ts`：两个新错误码的可恢复文案；
- `APP/src/views/MeView.vue`：家庭选择器（当前来源、多家庭下拉、切换二次确认、切换后重探能力），二次确认的家庭改用已选家庭。

测试（`npm run test`：20 files / **150 tests passed**，本 Story 新增 13 项）：

- `APP/src/data/httpProvider.test.ts`「多家庭选择与隔离（MOB-158）」7 项：未选择 fail-closed 且不发成员请求、单家庭自动选定、使用已选家庭、**列表顺序变化不改变已选家庭**、已选家庭消失报 `HOUSEHOLD_UNAVAILABLE` 且不发请求、无家庭仍报 `NO_HOUSEHOLD`、`listHouseholds` 只暴露 ID 与名称；
- `APP/src/data/index.test.ts`「家庭切换的上下文清理（MOB-158）」4 项：切换触发 `context` 清理、同值不重复清理、切换清空当前成员、家庭参与联机指纹；
- `APP/src/api/errors.test.ts` 2 项：两个新错误码的文案与"不暴露其它家庭"。

## 自动验证

在 `APP/` 执行（2026-08-21）：

```text
npx vue-tsc --noEmit                      # 无输出
npx vitest run                            # 20 files / 150 tests passed
npm run build                             # 成功
npx cap sync android                      # 成功
node scripts/audit-android-security.mjs   # status passed
git diff --check                          # 无输出
```

## 未完成证据

- 正式后端**多家庭**联调：需要一个身份被授权访问两个以上家庭的造数场景（当前 `seed-live-demo.mjs` 只建一个家庭），以及撤权、删除家庭后的回归；
- Android 真机与 PWA 上的单家庭、多家庭、切换、撤权、会话过期与离线恢复路径；
- 完成前 Issue 保持进行中。

## 安全、隐私与回滚

- 家庭选择不是授权：所有 API 仍由服务端做家庭/成员/字段/动作/目的/期限校验，本 Story 不改变任何服务端判定；
- 本机 `localStorage` 只多存一个家庭 ID，不存家庭名称、成员或任何健康数据，登出与清理时一并丢弃；
- 家庭 ID 不进 URL、通知或日志；
- 回滚：revert 本 PR 会回到"固定取列表第一个家庭"的旧行为——**这本身就是缺陷**，因此回滚后应立即改为单家庭部署或要求重新选择，不可长期停留在旧行为上。

