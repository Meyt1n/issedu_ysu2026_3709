# HCT-442：助手问题分类双通道（模型 + 词表兜底）

- FR/NFR：FR-08、NFR-04、NFR-05（安全边界）
- 用户价值：口语化高危用药问题不再因关键词漏配落入 GENERAL，同时保留确定性词表作为硬兜底
- 范围：在 HCT-402 微调可评估后，用本地分类头/小模型输出问题类型，与 `ai.safety.lexicon` 词表取并集（宁可多路由到 MEDICATION_SAFETY）
- 非目标：不把分类结果当诊断；不云端分类；不替代证据校验
- 风险：P1（安全路由）
- 依赖：HCT-402 指令数据与盲测交付物、现有 `classify_question` / `src/ai/safety/lexicon.py`
- 允许修改：`src/api/app/tool_call.py`、`src/ai/safety/`、相关 unit/safety 测试、本 Story、需求追踪矩阵
- 验收：Given 口语「吃错药了/药吃多了」与未见过的近义表述，When 分类，Then 进入 MEDICATION_SAFETY 或 URGENT；词表命中时模型失败仍正确路由；无模型时纯词表可运行
- 回滚：关闭模型分类开关，回退词表-only
- 状态：已批准待办（依赖 HCT-402，本增量不落地运行时模型分类）
