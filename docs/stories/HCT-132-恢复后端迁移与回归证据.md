# HCT-132：恢复后端迁移与回归证据

- Issue：[#132](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/132)
- FR/NFR：FR-02、FR-03、FR-08、NFR-01、NFR-03、NFR-04、NFR-06
- 状态：进行中
- 负责人：Shen-huang-123
- 复核人：Meyt1n 或其他未参与实现的维护者
- 风险：R3
- 前置依赖：PR #128 已合并；误删提交为 `97bfdfdd1f25c264da7ea62b9b8376dde433468d`
- 预计完成：2026-08-13
- 允许修改：被该提交删除的后端模块、迁移、测试、Story/ADR/复核记录和部署脚本；本 Story、HCT-405 Story 与需求追踪矩阵

## 用户价值

恢复已验收的事件恢复、视觉任务、人工复核、知识检索、助手安全和部署能力，使数据库迁移、运行时代码和关闭的 Story 重新一致，并解除 HCT-405 的后端集成阻塞。

## 范围

- 恢复误删的 outbox worker、红队模块、备份/恢复脚本和对应自动测试。
- 恢复 HCT-103、HCT-204、HCT-207、HCT-401、HCT-403 的历史 Alembic 分支。
- 使用无数据变更的 merge revision 合并恢复分支与 HCT-208/HCT-404 分支。
- 验证从任一历史 head、空库和已有事件数据库升级到单一 head。
- 恢复被误删的 Story、ADR 和复核记录，不夸大其验收状态。

## 明确不做

- 不改写或强推 `master` 历史。
- 不删除 HCT-208/HCT-404 的迁移或实现。
- 不新增医疗规则、诊断、处方、停药、换药或剂量判断。
- 不提交真实健康数据、药品图片、密钥、模型权重、日志或缓存。
- 不把单元测试、Mock 或迁移恢复宣称为 HCT-405 全链验收完成。

## Given / When / Then

- Given 数据库停留在恢复前的 HCT-403 head，When 升级到 `head`，Then HCT-208/HCT-404 分支被补齐且最终只有一个 Alembic head。
- Given 数据库停留在 HCT-404 head，When 升级到 `head`，Then事件恢复、视觉、复核和知识表被补齐且既有 HCT-208/HCT-404 表保留。
- Given 已有确认事件的 HCT-102/HCT-103 数据库，When 升级到 `head`，Then 事件序号和相关性字段被回填且不可变触发器生效。
- Given 恢复后的代码，When 运行定向回归，Then授权、事件、人工复核、红队与部署测试不因文件缺失而失败。

## 验收与证据

- [x] `0009_merge_backend_heads` 合并两条恢复历史；当前唯一 head `0010_hct405_review_wiring` 是其线性后继
- [x] 两个历史 head 均可升级到合并 head
- [x] HCT-103 已有事件迁移保留数据并阻止覆盖写
- [x] HCT-103/204/207/406/408 定向回归通过
- [x] Ruff 与除仓库间远程 SHA 检查外的全量 pytest 通过
- [ ] GitHub/内部平台 `master` 恢复同一 SHA 后，全量 pytest 通过
- [ ] HCT-405 Story 按真实 API/E2E 证据更新，不提前关闭 #70

远程 SHA 检查当前单独失败：GitHub `master` 为
`b4ab7a3b9c4c569b7544edab0cf921a0d494825c`，内部平台 `master` 为
`e38568b23c5428f6f3524d43ffa849b250c1ea9e`。两个提交包含相同会议文件，
但由两端分别直接提交而形成分叉；这不是本迁移恢复引入的测试失败。

## 验证命令

```powershell
uv run alembic heads
uv run ruff check src/api tests migrations scripts
uv run pytest tests/integration/test_hct132_migration_reconciliation.py
uv run pytest
git diff --check
```

## 回滚

在合并前直接丢弃该任务分支。合并后如出现未预期迁移问题，回退本 PR 的代码和 merge revision；已经应用任一历史分支的数据库不做破坏性降级，使用前向修复并保留现有事件、审计、知识和模型发布记录。
