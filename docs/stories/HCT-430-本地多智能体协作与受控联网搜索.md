# HCT-430：本地多智能体协作与受控联网搜索

## 任务元数据

- FR/NFR：FR-08、NFR-01、NFR-02、NFR-03、NFR-04、NFR-07
- 风险等级：R3（网络出口、健康数据边界、模型提示注入）
- 前置依赖：HCT-004、HCT-403、HCT-415
- Issue：#310
- 当前状态：已实现，待 PR 合并、维护者 R3 复核和真实环境演示验收

## 目标

为本地健康助手提供类似工作流智能体的协作体验，但不引入云端智能体或云端大模型。一次请求由本地 API 编排以下节点：

1. 本地路由智能体：确定问题类型和执行路径；
2. 本地数据库智能体：只能调用 HCT-403 已批准的授权只读工具；
3. 本地知识智能体：检索已审核的本地知识文档并绑定可核验片段；
4. 受控联网搜索智能体：仅在部署开关和本次请求两个条件同时满足时运行；
5. 本地综合智能体：通过回环地址的 Ollama 使用上述结果生成结构化回答。

“所有智能体本地运行”具体指编排、工具权限、数据库访问和 Ollama 推理均在本机 API/本机 Ollama 完成。联网搜索不是云端智能体：它是本机发起的受控外部工具请求，默认关闭，且外部结果不属于本地审核证据。

## 安全边界

- Ollama 地址必须是 `localhost`、`127.0.0.1` 或 `::1`；非回环模型地址直接降级，不允许把健康上下文发送到云端模型。
- 数据库智能体不生成 SQL，不接收模型自定义工具；只复用 `execute_whitelisted_tool`，再次校验 actor、家庭、成员、访问目的和只读范围。
- 联网搜索只接收脱敏后的用户问题；不得发送 actor/household/member ID、成员姓名、健康事件、家庭事实、药品扫描图片、OCR、报告、向量、对话上下文或数据库返回值。
- 联网搜索必须由 `AGENT_WEB_SEARCH_ENABLED=true` 和请求体 `allow_network_search=true` 双重开启；HTTPS 地址还必须命中 `AGENT_WEB_SEARCH_ALLOWED_DOMAINS`。
- 外部结果单独放在 `external_sources`，不能写入本地 `sources`/`citations`；用药安全问题即使有外部搜索结果，也仍要求本地审核知识片段，否则返回 `EVIDENCE_REQUIRED`。
- 搜索失败只影响联网搜索节点，不阻断本地数据库、知识和规则链路；模型失败、引用伪造、危险输出和非本地模型地址继续走结构化降级。
- 搜索结果页面只作为外部参考展示，不能提供购药、问诊导流或临床结论。

## 验收条件

- [x] API 提供多智能体模式和 `/api/v1/assistant/agents` 能力目录，明确所有智能体 `local=true`。
- [x] 助手响应返回 `orchestration_id`、`agent_trace`、`all_agents_local`、`network_used` 和外部参考列表。
- [x] 未选择家庭成员时，数据库智能体跳过，不扩大查询范围；选择成员后只调用 HCT-403 授权只读工具。
- [x] 联网搜索默认关闭；未双重开启时不会建立外部 HTTP 请求。
- [x] 搜索查询会移除 ID、手机号、邮箱等标识，且请求中不包含数据库/成员上下文。
- [x] 本地 Ollama 使用回环地址；用药安全问题必须经过本地知识引用校验。
- [x] 后端定向测试覆盖脱敏、HTML 结果解析、双重开关、域名白名单、只读工具范围和智能体目录。
- [x] 本地冒烟脚本与离线分类/编排自检证据已落盘（见 `docs/reviews/HCT-430-助手演示冒烟记录.md`）；问候快速路径可不依赖 Ollama。
- [ ] 维护者完成正式环境 R3 人工复核、真实 MySQL/浏览器端到端录屏与回滚演练签署（冒烟记录不替代此项）。

## 2026-08-25 增量：编排精炼与前端成熟化

在保持全部安全边界不变的前提下，对既有实现做了三类精炼：

1. **真正的条件路由**：路由结果现在会裁剪执行路径——普通问候直接由本地流程回复（不调用模型、工具或网络）；规则与证据问题在已选成员时以规则记录为准、跳过通用资料检索；紧急问题不等待外部搜索；未选成员时数据库智能体保持跳过。受控联网搜索只接收脱敏查询，因此与本地检索并行执行，数据库会话仍保持单线程。
2. **联网搜索健壮化**：搜索结果解析器容忍 DuckDuckGo 页面结构变化（标题容器、重定向链接兜底）并在页面损坏时安全降级；搜索成功但无结果时 trace 记为 `completed` 且摘要明确"未找到"；`/api/v1/assistant/agents` 新增 `web_search_ready`（部署开关与出口白名单同时满足才为 true）。默认 `AGENT_WEB_SEARCH_ENABLED=false` 不变。
3. **前端成熟化**：助手页去除假进度动画，改由响应中的真实 `agent_trace` 驱动状态；移除硬编码模型名，改为显示 API 返回的模型；流程面板默认折叠、文案产品化（"本地分析流程"、"健康参考 · 需人工确认"），不再向用户暴露"智能体/节点/Ollama"等内部术语。

对应测试：`tests/unit/test_hct430_local_agents.py`（新增路由计划、问候快速路径、解析容错、无结果 trace、目录就绪状态用例）与 `tests/contract/test_hct430_local_agents_contract.py`（新增 `web_search_ready` 契约断言）。

## 2026-08-25 增量：性能、流式体验与搜索后端抽象

1. **编排性能**：档案核对与资料检索在独立数据库会话中并行执行；用药安全问题在本地资料库无命中时提前返回 `EVIDENCE_REQUIRED`，跳过 Ollama 调用；`GENERAL` 类型默认跳过资料检索与外部搜索。
2. **流式体验**：新增 `POST /api/v1/assistant/chat/stream`（SSE），逐步推送 `trace` / `external_sources` / `token` / `done` 事件；前端助手页改走流式接口，流程进度与回答生成实时可见。
3. **搜索 Provider 抽象**：新增 `src/api/app/search_providers.py`，支持 `duckduckgo_html`（默认）与 `searxng`（`AGENT_WEB_SEARCH_PROVIDER=searxng`，URL 指向 SearXNG JSON API）；catalog 返回 `web_search_provider`。

对应测试：`tests/unit/test_hct430_search_providers.py`、用药安全提前短路与 GENERAL 路由用例、流式端点契约（`STREAM_REQUIRES_MULTI_AGENT`）。

## 2026-08-25 增量：流式语义、取消、证据压缩与搜索缓存

1. **流式语义**：模型草稿在服务端缓冲并完成校验后，才以最终 `answer` 文本推送 `token`；校验失败则推送受控降级文案，前端不再露出半截 JSON。
2. **取消与背压**：SSE 生成器在客户端断开时设置 `cancel_event`，停止 Ollama 流；前端用 `AbortController` 在清空会话、切换成员、新发送时取消上一次请求，流式失败时回退到 `/assistant/chat`。
3. **证据压缩**：`_compact_local_evidence` 按 `query_type` 裁剪字段，知识片段只保留 top-3 摘要。
4. **搜索缓存与排序**：脱敏 query 有短 TTL 缓存；结果按查询词重叠与医疗域名加权排序。
5. **脱敏审计**：有家庭上下文时写入 `AccessAudit`（`ASSISTANT`/`MULTI_AGENT_CHAT`），只记录问题类型、步骤状态与是否联网，不落库用户原问题。

## 2026-08-25 增量：阶段反馈、停止按钮与搜索限速

1. **阶段状态**：编排过程推送 `routing` / `retrieving` / `searching` / `generating` / `validating`；前端等待文案与流程步骤高亮跟随真实阶段，而不是五个节点同时“处理中”。
2. **停止按钮**：发送中可主动取消；清空会话 / 切成员仍会中止进行中的请求。
3. **搜索限速**：`AGENT_WEB_SEARCH_MIN_INTERVAL_SECONDS`（默认 1s）避免同一进程短时间重复打外部搜索；触发时联网步骤降级，本地链路继续。

## 2026-08-25 增量：随身版流式对齐、分类双通道与演示冒烟

1. **APP 助手**：`assistantChatStream` + 阶段文案、停止、联网勾选；失败回退非流式（MOB-150 / HCT-430）。
2. **HCT-442**：`src/ai/safety/classifier.py` 词表 + 可选回环 Ollama 分类，按严重度合并；默认 `AGENT_CLASSIFIER_ENABLED=false`。
3. **演示冒烟**：`scripts/hct430_assistant_demo_smoke.py` 生成 `docs/reviews/HCT-430-助手演示冒烟记录.md`（诚实区分 pass/skip；不宣称 R3 完成）。

## 2026-08-25 增量：路由解释、规则节点与会话运维

助手响应新增分类双通道明细、路由解释和合成前的脱敏证据预览；显式规则节点只调用确定性规则工具，档案节点增加授权只读的今日照护计划摘要。短期检索缓存按 actor、助手会话、家庭和成员隔离，命中前重新核验当前授权，并提供本人会话清理接口；联网搜索增加不含查询正文的运维指标接口。流式取消改为独立 `cancelled` 事件，Ollama 响应流会先关闭再终止。分类器和联网搜索默认关闭策略不变，外部结果仍不得进入本地引用。

## 2026-08-25 增量：Web / APP UX 与运维界面

Web 助手已接入会话隔离 ID、规则步骤、路由说明、合成前证据预览、显式取消语义和“按用药安全再查一次”；清空或切换成员会清理当前 actor 的服务端会话缓存并轮换 ID。知识页新增只读“联网搜索运行状态”卡，403 时安静显示无权提示。APP 客户端同步识别 `evidence_preview` / `cancelled`，取消不再回退非流式请求或保留半截回答，并提供最小证据预览与用药安全复核入口。对应 Web / APP 客户端单测与类型检查作为本增量自动证据；浏览器 E2E、Android 真机和正式 R3 演示仍未执行。

## 2026-08-25 增量：可启用性闭环（可发现、可配置、可演示）

针对「用户不知道联网搜索为何不可用、如何启用」的反馈，在不改变默认关闭策略的前提下补齐启用闭环：

1. **不可用原因外显**：`/api/v1/assistant/agents` 新增 `web_search_unavailable_reason`（`DEPLOYMENT_DISABLED` / `EGRESS_BLOCKED` / `OPT_IN_REQUIRED`）与 `web_search_enable_hint`（操作员指引文案）、`web_search_offline_fixture`；契约测试锁定枚举。
2. **离线教学夹具 provider**：新增 `AGENT_WEB_SEARCH_PROVIDER=fixture`（`src/api/app/search_providers.py::FixtureSearchProvider`），返回本机合成教学参考（保留域 `fixture.invalid`、`source=teaching_fixture`、自带「教学夹具，非真实网页/非医疗证据」标注），不构造 HTTP 客户端、不出网；trace 中该节点 `network_used=false`，egress guard 对 fixture 直接放行（无出口可管）；生产配置校验对 fixture 不再强制域名白名单。
3. **前端可发现**：助手页勾选禁用时按原因显示文案（部署未启用 / 出口白名单未通过），并提供「如何启用？」展开可复制的 `.env` 指引；fixture 就绪时显示「教学夹具 · 不出网」标识与专用说明。跳过状态的流程节点文案同样按原因区分。
4. **文档**：README「启用联网搜索与刷新知识库」短节 + 本地部署指南 §4.2（路线 A 夹具 / 路线 B 白名单联网 + 验证命令）。

安全边界不变：双重开启、脱敏查询、`external_sources` 与本地引用分离、失败不阻断本地链路、默认 `AGENT_WEB_SEARCH_ENABLED=false`。

对应测试：`tests/unit/test_hct430_search_providers.py`（fixture 离线性、主题匹配、限速豁免）、`tests/unit/test_hct430_local_agents.py`（catalog 原因/指引、fixture 节点 `network_used=false`）、`tests/contract/test_hct430_local_agents_contract.py`（原因枚举契约）、`src/web/src/assistant/webSearchAvailability.test.ts`（UI 文案映射）。浏览器 E2E 与正式 R3 演示仍未执行。

## 配置与回滚

默认配置保持安全：

```dotenv
AGENT_ORCHESTRATION_ENABLED=true
AGENT_WEB_SEARCH_ENABLED=false
AGENT_WEB_SEARCH_PROVIDER=duckduckgo_html
AGENT_WEB_SEARCH_URL=https://html.duckduckgo.com/html/
AGENT_WEB_SEARCH_ALLOWED_DOMAINS=html.duckduckgo.com
AGENT_WEB_SEARCH_CACHE_TTL_SECONDS=180
AGENT_WEB_SEARCH_MIN_INTERVAL_SECONDS=1
AGENT_CLASSIFIER_ENABLED=false
AGENT_CLASSIFIER_TIMEOUT_SECONDS=3
AGENT_RETRIEVAL_CACHE_TTL_SECONDS=120
```

演示联网搜索前，部署者显式配置搜索站点白名单（或改用 `AGENT_WEB_SEARCH_PROVIDER=fixture` 做完全离线的教学演示）；网页端每次请求仍需用户勾选“允许联网搜索”。如发现出口、隐私或模型边界问题，设置 `AGENT_WEB_SEARCH_ENABLED=false`，将 `AGENT_RETRIEVAL_CACHE_TTL_SECONDS=0`，或将 `agent_mode` 改为 `single`，即可关闭对应增量能力而不删除家庭数据、事件、知识文档或审计记录。

## 证据位置

- `src/api/app/local_agents.py`
- `src/api/app/search_providers.py`
- `src/api/app/egress_guard.py`
- `src/api/app/routes.py`
- `src/api/app/schemas.py`
- `src/web/src/views/AssistantView.vue`
- `APP/src/views/AssistantView.vue` / `APP/src/api/client.ts`
- `src/ai/safety/classifier.py`
- `scripts/hct430_assistant_demo_smoke.py`
- `docs/reviews/HCT-430-助手演示冒烟记录.md`
- `tests/unit/test_hct430_local_agents.py`
