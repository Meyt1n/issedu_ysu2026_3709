# MOB-159 手机首页季节健康资讯小卡片

| 字段 | 内容 |
|---|---|
| Issue | [#514](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/514) |
| FR/NFR | FR-08 本地证据型助手、NFR-02 隐私与数据权利、NFR-04 基础档降级可用、NFR-07 可访问与可理解 |
| 状态 | 进行中（移动端实现与自动测试已完成，待 PR/维护者复核及 Android/PWA 人工验收） |
| 关联 | HCT-444（[PR #430](https://github.com/Meyt1n/issedu_ysu2026_3709/pull/430) 首页健康新闻常驻展示、[PR #432](https://github.com/Meyt1n/issedu_ysu2026_3709/pull/432) 白名单抓取与缓存降级） |
| 风险 | R2（把资讯误读成医疗建议，或把过期缓存当实时结论） |
| 负责人 | Shen-huang-123 |
| 复核人 | 维护者合并时完成最终复核 |
| 允许修改 | `APP/src/views/TodayView.vue` 或独立资讯卡组件、`APP/src/data/`、`APP/src/api/`、能力探测、测试、本 Story、需求追踪矩阵 |

## 用户价值

网页端首页已有健康新闻常驻展示（HCT-444），移动端首页还没有对应入口。补一张季节健康资讯小卡片，消费家庭服务器已抓取并缓存的白名单资讯，让长辈在首页就能看到当季提示，并能一步跳到本地助手继续追问。

## 范围

- 首页新增季节健康资讯小卡片：标题、一句话摘要、来源名称、发布/抓取时间、缓存新鲜度。
- 卡片点击进入本地助手，并把该条资讯作为上下文带入提问草稿。
- 能力未声明、服务端无资讯、抓取失败或仅有旧缓存时的四种降级态文案。
- 长辈模式与特大字号下的卡片布局；TalkBack 朗读顺序与可聚焦区域。

## 非目标

- 不在 APP 内直接请求任何第三方资讯源；只消费家庭服务器受控结果。
- 不做资讯收藏、分享、评论或推送。
- 不改后端抓取、白名单与缓存策略（属 HCT-444）。
- 不做诊断/处方/停药/换药/剂量判断；不买药/问诊/广告导流。
- 健康数据默认不出网（联网搜索遵循 ADR-0007 显式 opt-in）。

## 验收（Given / When / Then）

1. Given 服务端返回白名单资讯且能力已声明，When 打开首页，Then 展示资讯卡片并同时显示来源与抓取时间。
2. Given 资讯含用药或诊疗措辞，When 展示，Then 只保留科普口径与查看来源入口，不生成停药、换药、剂量或诊断结论。
3. Given 服务端资讯不可用、超时或能力未提供，When 打开首页，Then 显示明确的不可用说明并保留首页其余功能，不展示空白卡片。
4. Given 只有过期缓存，When 展示，Then 明示这是缓存内容与其时间，不冒充实时资讯。
5. Given 点击卡片，When 进入本地助手，Then 提问草稿带上该条资讯标题与来源，用户可自行修改后再提交。
6. Given 长辈模式与特大字号，When 查看卡片，Then 文字不截断、触控区不小于既有标准，TalkBack 能按标题→摘要→来源顺序朗读。

## 安全、隐私与回滚

- 资讯不是医疗建议；卡片不得出现购药、问诊、导流入口。
- 不把成员姓名、健康事件或位置发送给任何外部资讯服务。
- 本机不缓存资讯正文之外的任何健康数据。
- 回滚：隐藏首页资讯卡片入口，首页其余模块不受影响。

## 当前实现与证据

- `APP/src/api/types.ts`、`APP/src/api/client.ts`：对齐 `GET /api/v1/health-news` 响应，只由家庭服务器提供远程结果。
- `APP/src/data/types.ts`、`APP/src/data/httpProvider.ts`、`APP/src/data/demoProvider.ts`：演示/联机 Provider 共享资讯契约；演示数据为本地季节日历，未访问网络。
- `APP/src/utils/healthNews.ts`：统一状态、缓存时间、来源行和降级文案；本地卡片只使用教学/生活照护口径。
- `APP/src/components/HealthNewsPanel.vue`、`APP/src/views/TodayView.vue`：今日页卡片、空态/异常保留首页、来源与时间展示、键盘/读屏顺序和触控目标。
- `APP/src/views/AssistantView.vue`：点击资讯只预填带标题与来源的可编辑草稿，不自动发送。
- 自动验证：`APP/npm run check`、`APP/npm run test`（33 个测试文件、296 项通过）、`APP/npm run build`。
- Android Debug APK：2026-08-28 使用完整 JDK 21 构建成功；产物 `APP/android/app/build/outputs/apk/debug/app-debug.apk`，SHA-256 `525EA42BF149240C621CEE61E0BC187CB20C0AB08D7607F97A00BC2905473144`。为启用 Debug-only WebView 网络策略，`APP/android/app/build.gradle` 显式开启 `BuildConfig` 生成。
- Android 静态安全审计：`APP/npm run audit:android-security` 通过；真实 Android 设备安装、首页/助手人工验收仍待 ADB 设备登记后执行。
- Android 真机抽查：2026-08-28 在荣耀 AAP-AN00（1272×2800）安装同一 APK，启动首页、滚动资讯卡片、进入助手及草稿预填均通过；ADB 使用 `ADB_LIBUSB=1` 识别复合 USB 接口。最新布局修复后的 APK SHA-256 为 `F3EF8E954E08B13E9B9BF2677E6A79A30A97847BFE4BB3B30408B78C8CAAA777`。
- 受控资讯边界：复用 `APP/scripts/mob171-weak-network-fixture.mjs` 的 `news-stale`/`news-error` 场景，并由 `npm run verify:health-news-fixture` 自动校验过期时间、来源标注、503 降级和请求标识；该证据与 MOB-165 #520 共享，夹具只返回虚构数据。
- 待补证据：真实后端白名单资讯与 stale 缓存截图、Android/PWA 特大字号及 TalkBack 人工验收；过期缓存刷新策略仍由 MOB-165（#520）单独完成。
