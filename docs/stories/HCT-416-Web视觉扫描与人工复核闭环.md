# HCT-416：Web 视觉扫描与人工复核可解释闭环

- Issue：[ #256 ](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/256)
- 绑定：FR-03、NFR-03、NFR-04、NFR-07
- 状态：已合并；网页扫描、原地重处理和人工复核交接已有自动化证据，真实账号/摄像头验收仍按 R3 保留
- 风险：R2；不改变“候选不等于健康事实”的边界

## 目标

把网页端视觉扫描从“能创建任务”补齐到可解释、可恢复的闭环：失败和超时给出结构化原因，允许安全重处理；成功后进入人工复核；复核确认、修正或跳过后主动刷新总览、风险、计划和健康图谱。

## 实现范围

- 服务端返回 `error_detail`：错误码、原始消息、是否可重试和下一步建议。
- `POST /api/v1/vision-tasks/{task_id}/retry` 只允许 `failed`/`timeout`，原地重新排队，保留任务 ID、文件和成员范围，不创建第二个任务。
- 扫描页展示结构化错误和重新处理按钮；任务从 queued/running 进入 succeeded 后自动进入人工复核中心。
- 识别详情补充 YOLO、OCR、条码、主数据版本和主数据候选；页面持续标注结果为候选。
- 家庭成员看到的是统一的人话失败原因、融合状态和下一步建议；不展示模型路径、worker 堆栈或内部错误码。
- 人工复核成功后发出本地刷新事件，已打开的总览、风险、计划和健康图谱重新读取服务端最终状态。

## 验收条件

- 失败/超时任务显示错误码、原因、下一步和可重试状态；重复点击不会创建第二个 `VisionTask`。
- 任务成功后只创建/读取待复核任务，不直接创建健康事实。
- 确认、修正、跳过后，健康事实、风险、时间线只从服务端重新读取；前端刷新事件不携带健康正文。
- 浏览器/API/单元测试覆盖结构化错误、原地重试、完成交接、复核后的刷新和证据版本展示。

## 验证命令

```text
uv run pytest tests/unit/test_hct204_vision_tasks.py -q
npm.cmd run test:web
npm.cmd run check:web
npm.cmd run build:web
git diff --check
```

HCT-489/HCT-490 的补充证据为 `src/web/src/vision/visionReasons.test.ts` 和
`tests/browser/hct416-vision-review.spec.ts`；浏览器回归覆盖失败任务人话提示、原地重试、不重复创建任务及复核交接。

2026-08-31 扫描页采集体验：本地预览等比例铺满虚线框；质量检查对可修复原因做一次本机增强后再复查。识别结果仍为候选，人工确认后才入档。

2026-08-31：扫描/复核原图上的 YOLO 定位框仅在置信度 ≥ 0.8 时绘制；低于阈值的 YOLO 证据仍留在侧栏清单，不改推理阈值、OCR 裁剪或健康事实写入。

## 回滚

回退重处理接口、扫描页入口、刷新事件和相关测试，保留原有任务查询与人工复核接口；不删除任务、原始文件或健康事实。
