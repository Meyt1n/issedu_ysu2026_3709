# MOB-177 手机端与网页端视觉系统对齐

| 字段 | 内容 |
| --- | --- |
| GitHub Issue | 待创建（本 Story 先记录实现边界，不得在 PR 中使用 `Closes`） |
| FR/NFR | FR-01 身份、家庭与成员级授权；NFR-07 可访问与可理解 |
| 状态 | 待创建 GitHub Issue / 真机验收完成（待 PR 人工复核） |
| 风险 | R2：视觉令牌重构可能降低对比度、缩小触控目标或遮挡安全区 |
| 负责人 | Shen-huang-123 |
| 复核 | 维护者在 PR 合并时进行人工复核 |

## 用户价值

让随身版与电脑端共享「暖阳纸笺」视觉语言：暖纸背景、松绿主色、陶土强调色、衬线标题、纸感卡片、极光漂移与萤火粒子，使两端看起来是同一产品，同时保留手机的单列阅读、底部导航和大触控目标。

## 范围与非目标

- 范围：`APP/src/style.css` 的设计令牌、全局卡片/按钮/标题/背景、`App.vue` 的极光/萤火装饰层、`public/sw.js` 的离线外壳缓存清单、底部导航视觉，以及 Android 原生容器的边到边显示和安全区传递；保留所有既有业务结构与路由。
- 非目标：不修改 API、数据请求、授权、健康文案、AI 能力、提醒逻辑或桌面端样式；不把桌面侧栏直接移植到手机。
- 不使用外部字体、图片或网络资源；健康数据默认不出网。

## 验收

1. Given 浅色主题，When 打开今日、家人、提醒、我的，Then 使用与网页端一致的暖纸/松绿/陶土色阶、卡片和标题层级。
2. Given 深色或高对比模式，When 切换主题，Then 文字和边界仍清晰，高对比模式关闭纯装饰与玻璃效果。
3. Given 320px、横屏、折叠屏或特大字号，When 浏览主要页面，Then 不产生横向滚动，底部导航和主要按钮仍可触达。
4. Given 减少动效，When 进入/切换页面，Then 不运行背景或卡片装饰动画。
5. Given Android 真机含状态栏与手势导航栏，When 打开随身版，Then 应用背景延伸至全屏，而正文和底部导航仍避开系统栏。
6. Given 正常动效，When 停留在移动端页面，Then 可看到低透明度极光缓慢漂移和萤火粒子上升，且不遮挡内容、不产生额外网络请求。

## 验证与回滚

- 验证：`cd APP && npm run check && npm run test && npm run build && npm run audit:responsive`，再执行桌面/PWA 和 Android 真机视觉走查。
- 回滚：恢复本 Story 所列移动端全局样式、`App.vue` 视觉改动与 Android `MainActivity`/主题设置；不影响业务数据或服务端。

## 2026-09-01 实现证据

- 对齐桌面端 `src/web/src/style.css` 的暖纸、松绿、陶土、风险/提示色阶、纸感阴影和衬线标题，不新增外部字体或网络资源。
- 移除随身版专属水彩背景 DOM，并将 Service Worker 外壳缓存升级为 `v4`，不再缓存已不用的背景图；接口和健康数据仍不进入缓存。
- 390px 宽度 PWA 走查确认主文档宽度等于视口宽度、今日首页卡片和底部导航均可见；仅使用内置演示数据。
- `npm run check`、`npm run test -- --reporter=dot`（39 个测试文件、356 项）、`npm run build`、`npm run audit:responsive`、`npm run audit:pwa` 与 `git diff --check` 均通过。
- 荣耀 Android 真机使用仅含虚构数据的离线演示模式走查今日、家人、提醒、我的：均无横向溢出，底部导航可见；深色、高对比和减少动效均正确生效。
- Android 原生容器已改为边到边显示，并把系统栏安全区换算为 CSS 变量。真机复测 WebView 高度由 739 CSS px 恢复至 800 CSS px（设备可用全高 801 CSS px），状态栏和手势栏不再留下空白区域，正文和底部导航仍避让安全区。
- `cd APP/android && ./gradlew.bat assembleDebug --no-daemon --console=plain` 构建成功，调试包已安装到真机复测。
- 真机 WebView 复测极光 3 个节点和萤火 8 个节点均已加载，动画名称为 `ambient-aurora-drift`；开启减弱动效后 `data-motion="reduced"` 且两层均为 `display:none`。

## 2026-09-01 明亮主题补充

- 提亮默认浅色令牌：暖纸背景改为 `#fffaf1`，卡片改为高不透明度白色，松绿主色与边框层级同步提亮；同时将深色模式从近黑森林夜调整为可分层的深松绿色，避免 OLED 黑底吞没卡片边界。
- `SystemBars.setStyle` 会随主题切换同步 Android 状态栏/手势栏图标明暗；PWA 在无原生插件时安全降级，不改变现有主题选择和无障碍行为。
- 在荣耀 AAP-AN00 上安装提交 `0d67b67` 构建的 Debug APK（SHA-256：`BEE01D39EF9B710EE1633C6F6B7A3DB714701ECE5ACC9D2BD5BC1B2EA3FC3A62`）并切换到浅色主题，截图确认背景、卡片、底部导航和状态栏图标均清晰可读。
- 真机 WebView 复核：`theme=light`、`innerWidth=363`、`innerHeight=800`、`screenHeight=801`、`bodyScrollWidth=363`、`bodyClientWidth=363`；未发现横向溢出。该复核仍不替代折叠屏、PWA 安装态和发布门禁签收。
