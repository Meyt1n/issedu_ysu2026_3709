# 前端项目完整进度介绍

> 项目：issedu_ysu2026_3709（HomeCare Twin）
>
> 汇报范围：全组前端工作
> 整理日期：2026 年 8 月 13 日

## 一、整体进度概述

前端工作已完成从工程基础、接口契约到业务页面、视觉交互与自动化质量保障的连续建设。主干中当前同时保留 Vue 业务前端和 React 展示/模型实验室前端两条实现线：Vue 线承接授权、健康、风险、视觉质量与 HCT-405 业务闭环；React 线承接完整界面展示、认证、仪表盘、健康页面、设置页面及模型实验室演示。

基于主干 Git 历史，检索到 32 条涉及 `src/web`、浏览器端测试、Playwright 配置或前端依赖的提交记录。前端相关提交由 Meyt1n、jin-123-zip、ry12-20、zhang 等成员共同完成，形成了“功能实现 + 接口接入 + 页面测试 + 浏览器验收 + 可访问性检查”的协作闭环。

## 二、前端工程基础与开发体系

1. **P0 工程底座**：提交 `ae09a7a` 建立 HomeCare Twin 工程基础，包含 Vue/Vite 前端入口、页面样式、Docker Web 构建配置、启动脚本、依赖管理与 CI 配套，为后续多人并行开发提供一致的启动与交付环境。
2. **API 契约基础层**：提交 `906b99a` 完成前端 API 客户端、类型定义和接口使用方案。该层将前后端数据交互收敛为统一的调用方式，使业务页面可以复用类型和请求能力，降低页面直接拼接请求造成的维护成本。
3. **前端质量闭环**：提交 `5f970f6` 补充视觉质量面板、质量视图逻辑、客户端测试和配置适配，将质量检查结果沉淀为可视化、可测试的前端能力，而不是只存在于后端日志或人工检查中。

## 三、Vue 业务前端建设

### 1. 家庭授权与安全边界

HCT-102 先完成授权安全边界和前端 API/类型扩展（`3ac8fc2`）；随后交付照护者授权工作区（`815209b`，PR #89），包括 App 页面、授权视图逻辑、接口调用、单元测试、依赖与样式适配；最后通过 PR #90 修复授权页面布局审查意见（`0b4af70`）。该模块形成“安全规则可执行、授权信息可查看、用户操作可完成”的前端入口。

### 2. 健康看板、风险卡与照护操作

HCT-306 家庭健康看板（`c1657ce`，PR #99）完成看板页面、接口接入、客户端测试和样式；HCT-307 风险证据卡（`31f43ae`，PR #98）补充风险视图、风险卡呈现、类型/客户端与测试；`f20ce75` 在主界面加入照护计划和风险评估控制项。三部分共同将健康状态、风险依据和后续照护操作连接为面向用户的业务闭环。

### 3. 视觉质量与安全复核接入

HCT-405 的视觉融合与安全复核生命周期接入（`a4a6e3a`）同步调整 App、视觉质量面板、质量视图及测试，前端能够承接视觉任务状态、质量判断和安全复核结果。该改动把复杂后端流程转化为前端可观察、可确认的工作界面，是业务功能从接口到用户侧的关键落点。

## 四、测试、联调与可访问性保障

### 1. HCT-405 端到端质量链路

提交 `5336f90` 建立 HCT-405 E2E 基线，并补充客户端测试与验收说明；`4c2480df`（PR #130）引入 Playwright 配置和浏览器可见工作流测试；`d8b451e`（PR #139）进一步增加真实本地 API 的浏览器 E2E 覆盖。三阶段覆盖页面渲染、用户操作、异步请求、接口响应、状态更新和结果反馈，使关键流程可重复执行、可回归验证。

### 2. 测试稳定性与安全降级

围绕 HCT-405，团队完成事件/知识模块兼容性修复、合成测试载荷 CI 适配、后端 E2E 自动化及安全降级（`80e3d65`、`1861702`、`46f6eaa`）。前端测试由此能够在持续集成和真实联调环境中运行，出现接口或数据异常时也有明确的可验证行为。

### 3. 无障碍专项验收

HCT-409（`ed24bd5`，PR #138）完成前端可访问性修复，并新增 axe 浏览器自动化测试。修改覆盖 App、视觉质量面板、质量视图、样式和相应测试。该专项让页面不仅验证功能正确，也能持续检查无障碍问题，提高核心交互对不同用户的可用性。

## 五、React 展示前端与模型实验室

### 1. React 前端演示体系

仓库在 `src/web/react` 下形成独立的 React/Vite 展示前端，包含认证页、仪表盘、健康页面、设置页面、核心业务页、页面视觉资源及路由测试。提交 `adebfad` 使 React 前端演示能力在 HCT-407 迭代中得到保留，确保模型实验室等新能力有可演示的用户界面。

### 2. HCT-407 模型实验室

提交 `9c2f62b` 完成模型实验室的版本指标与回滚演练界面，补充页面逻辑、样式、Playwright 配置和模型实验室浏览器测试。该模块把模型版本、指标和回滚等技术管理事项呈现在前端，方便演示与验收模型生命周期管理能力。

### 3. 页面和路由测试完善

提交 `ef6abf` 与 `01c0598` 更新 React 前端和路由测试，涉及认证页、仪表盘、核心页面、健康页面、设置页面、全局样式和 app-routes 测试。中间出现的回滚提交 `f0d574c` 反映团队通过主干校验后重新应用有效改动，最终以 `01c0598` 的重新应用版本进入主干。

## 六、协作模式与当前成果

当前前端协作已具备以下成果：

1. 业务模块按照独立功能分支和 Pull Request 推进，授权、健康看板、风险卡、E2E、无障碍等工作均有可追踪记录。
2. 前端接口层、类型定义和页面测试同步维护，降低接口改动的联调风险。
3. 关键用户流程已覆盖浏览器 E2E、真实 API E2E 和 axe 无障碍测试。
4. Vue 业务实现与 React 展示实现并行存在，可分别满足实际业务闭环和汇报演示需求。

## 七、汇报可直接使用的前端介绍

前端组目前已完成从工程底座、统一 API 接入到授权管理、家庭健康看板、风险证据展示、照护操作、视觉质量复核和模型实验室等核心能力建设。我们不仅实现页面，还为关键流程补齐了接口测试、浏览器端 E2E、真实 API 联调和无障碍自动化检查。当前前端已形成可展示、可联调、可回归的交付状态，后续将围绕主干稳定性、异常态补齐和关键路径覆盖持续完善。

## 八、后续计划

1. 持续执行 HCT-405 真实 API 与浏览器端回归场景，保障主干迭代稳定性。
2. 扩展 axe 无障碍测试覆盖范围，并补齐授权、看板、风险等页面的异常态和空态。
3. 持续统一 Vue 业务线与 React 展示线的接口说明、验收路径和演示数据，使汇报展示与实际业务能力保持一致。

## 九、仓库核验依据

仓库：`github.com/Meyt1n/issedu_ysu2026_3709`。检索范围：`master` 分支中 `src/web`、`tests/browser`、Playwright 配置、`package.json` 与 `package-lock.json` 相关历史；统计结果为 32 条前端范围提交。文中任务名称、提交号和功能范围均根据本次 Git 历史与当前文件树整理。
