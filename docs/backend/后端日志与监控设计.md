# 后端日志与监控设计

> 本文档是家健镜系统日志与监控的完整设计说明，覆盖日志规范、监控指标、告警规则、链路追踪。

## 1. 日志设计

### 1.1 日志级别

| 级别 | 说明 | 示例 |
| --- | --- | --- |
| DEBUG | 调试信息 | 函数入参出参、SQL 语句 |
| INFO | 正常运行信息 | 请求开始结束、用户登录 |
| WARNING | 警告信息 | 重试、降级、慢查询 |
| ERROR | 错误信息 | 异常、请求失败 |
| CRITICAL | 严重错误 | 系统不可用、数据丢失 |

### 1.2 日志格式

```json
{
  "timestamp": "2026-09-04T10:30:00.123Z",
  "level": "INFO",
  "service": "homecare-backend",
  "trace_id": "abc123",
  "user_id": "user_001",
  "household_id": "hh_001",
  "method": "GET",
  "path": "/api/medicines",
  "status_code": 200,
  "duration_ms": 45,
  "message": "获取药品列表成功",
  "extra": {}
}
```

### 1.3 日志配置

```python
import logging
import json
from pythonjsonlogger import jsonlogger

class RequestIdFilter(logging.Filter):
    def filter(self, record):
        record.trace_id = getattr(context_var, "trace_id", "-")
        record.user_id = getattr(context_var, "user_id", "-")
        return True

def setup_logging():
    logger = logging.getLogger()
    logger.setLevel(logging.INFO)

    handler = logging.StreamHandler()
    formatter = jsonlogger.JsonFormatter(
        "%(asctime)s %(levelname)s %(name)s %(trace_id)s %(user_id)s %(message)s"
    )
    handler.setFormatter(formatter)
    handler.addFilter(RequestIdFilter())
    logger.addHandler(handler)
```

### 1.4 请求日志中间件

```python
@app.middleware("http")
async def log_requests(request, call_next):
    start_time = time.time()
    trace_id = str(uuid.uuid4())
    context_var.trace_id = trace_id

    logger.info(
        f"请求开始 {request.method} {request.url.path}",
        extra={
            "method": request.method,
            "path": request.url.path,
            "query": dict(request.query_params),
        },
    )

    response = await call_next(request)
    duration = int((time.time() - start_time) * 1000)

    logger.info(
        f"请求结束 {request.method} {request.url.path}",
        extra={
            "method": request.method,
            "path": request.url.path,
            "status_code": response.status_code,
            "duration_ms": duration,
        },
    )

    response.headers["X-Trace-Id"] = trace_id
    return response
```

### 1.5 敏感信息脱敏

```python
def sanitize_log(data):
    sensitive_fields = ["password", "token", "phone", "id_card", "email"]
    if isinstance(data, dict):
        return {
            k: "***" if k in sensitive_fields else sanitize_log(v)
            for k, v in data.items()
        }
    if isinstance(data, list):
        return [sanitize_log(item) for item in data]
    return data
```

## 2. 监控指标

### 2.1 业务指标

| 指标 | 说明 | 采集方式 |
| --- | --- | --- |
| 活跃用户数 | 日活跃用户 | 统计 API 调用 |
| 药品录入数 | 每日新增药品 | 事件统计 |
| 用药提醒数 | 每日提醒次数 | 事件统计 |
| 服药依从率 | 已服/应服比例 | 统计计算 |
| 风险识别数 | 每日风险事件数 | 事件统计 |
| 视觉识别准确率 | 识别正确/总数 | 人工标注对比 |

### 2.2 系统指标

| 指标 | 说明 | 阈值 |
| --- | --- | --- |
| CPU 使用率 | 服务器 CPU | >80% 告警 |
| 内存使用率 | 服务器内存 | >85% 告警 |
| 磁盘使用率 | 磁盘空间 | >90% 告警 |
| 网络流量 | 入站出站流量 | 异常波动告警 |
| 连接数 | 数据库连接数 | >80% 告警 |

### 2.3 应用指标

| 指标 | 说明 | 阈值 |
| --- | --- | --- |
| QPS | 每秒请求数 | 监控趋势 |
| 响应时间 | P50/P95/P99 | P95 > 1s 告警 |
| 错误率 | 5xx 比例 | >1% 告警 |
| 慢查询 | 数据库慢查询 | >500ms 记录 |
| 缓存命中率 | 缓存命中比例 | <80% 告警 |
| 队列长度 | 消息队列长度 | >1000 告警 |

### 2.4 Prometheus 指标

```python
from prometheus_client import Counter, Histogram, Gauge

request_count = Counter(
    "http_requests_total",
    "Total HTTP requests",
    ["method", "path", "status"],
)

request_duration = Histogram(
    "http_request_duration_seconds",
    "HTTP request duration",
    ["method", "path"],
    buckets=(0.01, 0.05, 0.1, 0.5, 1, 2, 5, 10),
)

active_users = Gauge(
    "active_users",
    "Current active users",
)

error_count = Counter(
    "errors_total",
    "Total errors",
    ["type", "module"],
)
```

## 3. 告警规则

### 3.1 告警级别

| 级别 | 说明 | 响应时间 | 通知方式 |
| --- | --- | --- | --- |
| P0 | 系统不可用 | 立即 | 电话 + 短信 + 邮件 |
| P1 | 核心功能异常 | 15 分钟 | 短信 + 邮件 |
| P2 | 非核心功能异常 | 1 小时 | 邮件 |
| P3 | 性能下降 | 4 小时 | 邮件 |

### 3.2 告警规则示例

```yaml
groups:
  - name: homecare-alerts
    rules:
      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) / rate(http_requests_total[5m]) > 0.05
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "错误率过高"
          description: "过去 5 分钟错误率超过 5%"

      - alert: SlowResponse
        expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "响应时间过长"
          description: "P95 响应时间超过 1 秒"

      - alert: HighCPU
        expr: cpu_usage > 80
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "CPU 使用率过高"

      - alert: DiskFull
        expr: disk_usage > 90
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "磁盘空间不足"
```

## 4. 链路追踪

### 4.1 OpenTelemetry 集成

```python
from opentelemetry import trace
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import BatchSpanProcessor
from opentelemetry.exporter.jaeger.thrift import JaegerExporter

def setup_tracing():
    tracer_provider = TracerProvider()
    jaeger_exporter = JaegerExporter(
        agent_host_name="localhost",
        agent_port=6831,
    )
    tracer_provider.add_span_processor(
        BatchSpanProcessor(jaeger_exporter)
    )
    trace.set_tracer_provider(tracer_provider)
```

### 4.2 自定义 Span

```python
tracer = trace.get_tracer(__name__)

async def process_medicine(medicine_id):
    with tracer.start_as_current_span("process_medicine") as span:
        span.set_attribute("medicine.id", medicine_id)

        with tracer.start_as_current_span("query_database"):
            medicine = await db.get(Medicine, medicine_id)

        with tracer.start_as_current_span("check_interactions"):
            interactions = await check_interactions(medicine)

        return medicine
```

## 5. 日志检查清单

- [ ] 日志级别规范
- [ ] 结构化日志（JSON）
- [ ] Trace ID 贯穿
- [ ] 敏感信息脱敏
- [ ] 请求日志记录
- [ ] 错误日志完整
- [ ] 日志轮转
- [ ] 日志集中收集
- [ ] 监控指标覆盖
- [ ] 告警规则配置
- [ ] 链路追踪
- [ ] 仪表盘配置

---

*日志与监控是系统的眼睛。全面的可观测性，让问题早发现、早定位、早解决。*
