# MOB-172 Android 真机局部走查记录（2026-08-31）

> 对应 Issue [#527](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/527)。本记录只记录已连接荣耀真机上的通知渠道与隐私配置事实，不替代六状态到达矩阵、锁屏截图或设备持有人签收。

## 环境与数据边界

| 字段 | 记录 |
| --- | --- |
| 设备 | 荣耀 AAP-AN00 |
| Android | 16 |
| ROM/系统版本 | AAP-AN00 10.0.0.160(SP1C00E160R501P1) |
| Android System WebView | 138.0.7204.179 |
| 应用形态 | `com.homecaretwin.companion`，Capacitor Debug APK，versionName `1.0` |
| 屏幕 | 1272×2800，非折叠设备 |
| 数据模式 | 演示模式，虚构计划与成员；未使用真实健康数据 |

设备上已安装的 APK 关于页显示构建信息为 `2026/8/31 09:55:14`、提交 `64f8ec5`，不是本分支新构建；因此本记录不把它写成候选发布包。测试结束前未修改系统通知、勿扰或电池设置。

## 已观察到的 Android 通知事实

打开应用并进入今日页后，应用同步计划提醒并创建了明确的私有通知渠道。只读 `dumpsys notification --noredact` 观察到：

| 项目 | 观察结果 | 结论 |
| --- | --- | --- |
| 渠道 ID/名称 | `hct_reminder` / `计划提醒` | 通过：使用显式应用渠道 |
| 锁屏可见性 | `mLockscreenVisibility=-1000`（Android `VISIBILITY_PRIVATE`） | 局部通过：渠道默认不公开通知正文 |
| 通知权限 | `POST_NOTIFICATIONS` 已授权；应用通知重要性为 `DEFAULT` | 通过：权限状态可被系统识别 |
| 应用内提示 | 今日页显示“通知不会显示成员姓名、药名、风险详情或健康正文。” | 通过：隐私边界文案可见 |
| 同步状态 | 显示“已按服务端计划安排本地提醒。” | 通过：文案描述为“安排”，未声称已送达 |

## 载荷/到达验证边界

- 通过 WebView 调用短延迟虚构通知进行到达验证时，原生桥调用在 1.5 秒内未返回，未获得可复核的“已送达”结果；没有把这次尝试记为通过，也没有留下测试通知。
- 未在本次局部走查执行前台、后台、锁屏、息屏、划掉、重启、电池优化和勿扰模式 12 条用例，也未保存锁屏/通知栏截图；这些必须由设备持有人在同一 APK 上补齐。
- 未读取或记录设备序列号、真实通知正文、token、服务器完整响应或真实成员信息。

## 代码与自动检查

本分支另修复 `ReminderStatusCard.vue` 展开指引使用未定义 `--line` 变量的问题，改为设计系统已有的 `--c-line`，不改变提醒时间窗、通知权限或载荷策略。

| 检查 | 结果 |
| --- | --- |
| `cd APP && npm run check` | 通过 |
| `cd APP && npm run test -- --reporter=dot` | 通过，39 个测试文件、355 项测试 |
| `cd APP && npm run build` | 通过 |
| `cd APP && npm run privacy:scan` | 通过 |
| `cd APP && npm run audit:android-security` | 通过 |
| `git diff --check` | 通过（仅有 Git 的 LF→CRLF 提示） |
| `npm run verify:android-reminder-evidence` | 空白矩阵按设计拒绝，不能冒充真机签收 |
| APK 重建 | 当前环境缺少脚本要求的 JDK 21–24，未重建 |

## 与 MOB-172 矩阵的对应关系

| 范围 | 本次结论 | 说明 |
| --- | --- | --- |
| 通知渠道与隐私默认值 | 局部通过 | 私有渠道、最小化应用内文案和权限状态已观察 |
| REM-01～REM-12 到达矩阵 | 未验证 | 缺少可复核触发时间、系统状态和截图/录屏 |
| PRIV-01～PRIV-02 锁屏专项 | 未验证 | 未执行锁屏/通知历史截图签收 |
| 厂商电池、自启动、勿扰 | 未验证 | 未改变系统设置，未做对照实验 |

## 发布边界与回滚

- 本记录不改变 MOB-172 矩阵的“待验收”结论；没有完整到达与隐私证据前不得把通知说成已送达。
- 回滚本次代码只需恢复 `ReminderStatusCard.vue` 的分隔线变量；通知渠道、调度器和已有提醒列表不受影响。
- 完整验收仍需设备持有人补齐 14 条用例、锁屏专项、APK SHA-256 和脱敏附件哈希，再运行 `npm run verify:android-reminder-evidence`。
