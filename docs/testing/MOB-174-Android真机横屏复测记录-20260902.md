# MOB-174 Android 真机横屏复测记录（2026-09-02）

> 对应 Issue [#529](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/529)。本记录只覆盖非折叠荣耀设备的横竖屏与助手草稿保持，不替代折叠外屏/内屏、逐页截图、TalkBack 或设备持有人最终签收。

## 环境与数据边界

| 字段 | 记录 |
| --- | --- |
| 设备 | 荣耀 AAP-AN00 |
| Android | 16 |
| Android System WebView | 138.0.7204.179 |
| 应用形态 | `com.homecaretwin.companion`，Capacitor Debug APK |
| APP commit | `84d1dce`（当前 `master`） |
| APK SHA-256 | `7C30F6F4B2975176F555A6404EC1387327E7516E7E92924C96BB0D90D1B8C813` |
| 数据模式 | 演示模式，虚构数据；未使用真实家庭健康数据 |
| 设备形态 | 非折叠设备，物理尺寸 1272×2800 |

## 安装与启动

- 使用 USB ADB 安装当前构建包，安装结果为 `Success`。
- 启动后焦点位于 `com.homecaretwin.companion/.MainActivity`，助手页面及路由可正常加载。
- 测试结束已恢复系统自动旋转，未修改设备数据或应用业务状态。

## 横竖屏布局边界

通过 WebView 只读调试接口读取视口、根文档和滚动状态：

| 形态 | 路由 | CSS 视口 | 根 `scrollWidth/clientWidth` | `body.scrollWidth` | `window.scrollX` | 结果 |
| --- | --- | --- | --- | --- | --- | --- |
| 竖屏 | 今日 | 363×800 | 363/363 | 363 | 0 | 通过 |
| 横屏 | 今日 | 800×363 | 800/800 | 800 | 0 | 通过 |
| 横屏 | 助手 | 800×363 | 800/800 | 800 | 0 | 通过 |

横屏读取到 `window.innerWidth=800`、`innerHeight=363`，没有根文档横向溢出或可见横向滚动位置。

## 助手草稿旋转保持

1. 进入助手页，在输入框写入脱敏教学文本「横屏状态保持演示问题」。
2. 锁定横屏后重新读取 WebView 状态：输入值仍为原文，路由保持 `#/assistant`，未触发发送。
3. 恢复自动旋转；未产生重复请求或业务写入。

## 与 Story 验收条件的对应关系

| 条件 | 本次结果 | 结论 |
| --- | --- | --- |
| 主要页面旋转后无裁切、无横向滚动 | 今日/助手路由根宽度与视口一致，`scrollX=0` | 非折叠设备局部通过 |
| 折叠外屏/内屏切换保持状态 | 当前设备不是折叠屏 | 未验证 |
| 写操作不重复提交、不丢结果 | 本次仅验证助手草稿，未执行任务确认或上传 | 未验证 |
| 特大字号横屏文字与触控区可读 | 沿用 2026-08-31 局部记录，未在本次逐页复核 | 待签收 |
| 每种形态每页截图并由设备持有人签收 | 本次未生成完整发布门禁截图包 | 待验收 |

## 发布边界

- 本记录证明当前 `master` 构建包在荣耀非折叠设备上横竖屏边界和助手草稿保持正常。
- 不能据此关闭 MOB-174：折叠外屏/内屏、铰链安全区、逐页截图、TalkBack、高对比/减少动效组合与设备持有人签收仍需真实设备完成。
- 若后续真机发现布局回归，可回滚 `APP/src/style.css` 中 MOB-174 响应式规则，不改变业务数据和授权逻辑。
