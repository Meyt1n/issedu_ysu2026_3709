# MOB-160～MOB-175 Android 真机综合走查记录（2026-09-01）

> 本记录用于补充移动端 Issue 的同日真机事实。它只记录当前荣耀设备、当前安装包和演示数据可复核的观察，不把演示模式、旧 APK 或自动化结果写成正式发布通过。正式发布仍以设备持有人在受控后端、最终候选 APK 和对应验收矩阵上的签收为准。

## 1. 环境与边界

| 项目 | 记录 |
| --- | --- |
| 设备 | 荣耀 AAP-AN00（实体设备） |
| Android / WebView | Android 16；Android System WebView 138.0.7204.179 |
| 屏幕 | 1272×2800；非折叠设备 |
| 应用 | `com.homecaretwin.companion`；Capacitor WebView；versionName `1.0`、versionCode `1`、targetSdk `36` |
| 连接 | USB ADB，设备状态 `device`，LIBUSB backend；设备序列号不写入仓库 |
| 数据 | 仅虚构演示成员、计划、风险和知识条目；未使用真实健康数据 |
| 当前安装包 | `lastUpdateTime=2026-08-31 11:40:03`；不是本次分支重建的候选 APK |

当前检出分支的 MOB-160～MOB-175 业务代码来自最新 `master` 代码线；手机上的安装包早于 MOB-169 等后续提交。尝试生成增量 APK 时，Vite 构建和 Capacitor 同步成功，但 Android Gradle 构建因本机没有 JDK 21 toolchain 失败，因此下面将“源码/自动化通过”和“当前旧包观察”分开记录。

## 2. 自动化与安装前检查

在 `APP/` 目录执行，结果如下：

| 检查 | 结果 |
| --- | --- |
| `npm run test -- --reporter=dot` | 通过，39 个文件、355 项测试 |
| `npm run check` | 通过 |
| `npm run audit:responsive` | 通过；横屏、低高度和折叠屏规则存在 |
| `npm run privacy:scan` | 通过；未发现密钥/签名材料 |
| `npm run audit:android-security` | 通过；发布清单不开放明文网络，备份/设备转移受限 |
| `npm run android:sync:debug` | 通过；当前 `dist` 已同步到 Capacitor |
| `gradlew assembleDebug --no-daemon` | 未通过；`@capacitor/local-notifications` 要求 JDK 21，本机仅发现 JDK 24/JBR/JDK8 |

因此本次不把旧包行为标注为“修复后 APK 已验证”，也不提交 APK、签名材料、设备序列号或完整系统日志。

## 3. 页面路由综合走查

通过 Android WebView 只读诊断依次加载主要路由，页面标题和关键中性文案均出现，未执行会改变服务端事实的写操作：

| 路由 | 观察到的关键内容 | 结论 |
| --- | --- | --- |
| `#/` | “今日 · 家健镜随身版”“今日概览”“计划提醒”“健康资讯/环境” | 演示加载通过 |
| `#/scan` | “拍药盒”、相册/质量提示 | 演示入口通过 |
| `#/family` | 家人列表与成员入口 | 演示加载通过 |
| `#/family/m-wang` | 成员详情、用药/时间线 | 演示加载通过 |
| `#/alerts` | “提醒”“服务端风险审计”及审计字段 | 演示字段通过 |
| `#/alerts/m-wang/rule-expiry-01` | “风险依据”“服务端审计信息” | 演示详情通过 |
| `#/assistant` | 语音助手、联网范围说明、停止/结束、输入框 | 演示交互入口通过 |
| `#/knowledge` | 已批准知识条目列表 | 演示列表通过 |
| `#/knowledge/demo-doc-medication-basics` | 来源、许可、索引版本、生效时间和正文分块 | 已批准详情通过 |
| `#/knowledge/demo-doc-staging-draft` | “待批准草稿”“不可查看”，未出现草稿正文 | 未批准正文过滤通过 |
| `#/me` | “联机三步”、服务器地址、家庭选择、自检入口 | 演示入口通过 |
| `#/me/accessibility` | 长辈模式、高对比、减少动效、字号设置 | 设置入口通过 |
| `#/me/voice-check` | 语音自检页与无麦克风降级提示 | 降级入口通过 |
| `#/me/privacy` | 本地数据管理、清理/权利说明 | 隐私入口通过 |
| `#/help` | 紧急求助与拨号提示 | 演示入口通过 |

### 横屏与状态保持

临时锁定横屏检查上述 11 个主要路由后恢复系统自动旋转。横屏 CSS 视口为 `761×302`，每个路由的根 `scrollWidth/clientWidth` 均为 `761/761`，`window.scrollX=0`；未发现根文档横向溢出。助手页输入脱敏草稿后旋转，草稿仍保留且未触发发送。该设备不是折叠屏，不能代替内/外屏和铰链安全区签收。

## 4. 按 Issue 的可复核观察

| Issue | 本次真机观察 | 当前结论 |
| --- | --- | --- |
| MOB-160 / #515 | 助手气泡、输入框、时间标签、长文本/URL 换行及停止/结束入口可加载；本次未调用真实联机模型 | 演示 smoke 通过，发布矩阵仍待最终 APK/PWA |
| MOB-161 / #516 | 联网搜索开关默认关闭且在演示能力下禁用；出网范围、人工确认和本地知识说明可读 | 演示隐私语义通过，真实 provider 限速/超时未验证 |
| MOB-162 / #517 | 已批准条目可读来源/版本/分块；待批准草稿未展示正文 | 演示只读/过滤通过，受控 403、TalkBack 待签收 |
| MOB-163 / #518 | 今日页环境行动卡和虚构天气/季节资讯可见 | 仅演示数据通过，真实天气授权、超时、限速未验证 |
| MOB-164 / #519 | “我的”页进入联机三步清单及服务器/家庭/自检入口可见 | 入口通过，多家庭/拒绝/失败重试矩阵未验证 |
| MOB-165 / #520 | 今日页健康资讯卡可见；代码路径包含过期提示和刷新入口 | 当前包未做受控 TTL/失败刷新，不能标正式通过 |
| MOB-166 / #521 | 助手页显示麦克风未开启的文字降级；系统 `RECORD_AUDIO` 为 `ignore`，未擅自拉起权限 | 降级语义通过，15 秒停顿/TTS/来电抢占未验证 |
| MOB-168 / #523 | 停止/结束相关控件可见；演示模式没有真实流式模型可取消 | 控件 smoke 通过，真实流中断待验证 |
| MOB-169 / #524 | 当前旧 APK 登录页未包含后续刷脸引导；源码已有 fail-closed 卡片（不请求摄像头、不上传/落盘） | 旧包不具备代表性，必须用 JDK 21 重建包复验 |
| MOB-171 / #526 | 演示加载可看到列表骨架/状态播报；未注入超时、504、断网或连续重试 | 局部通过，弱网矩阵待维护者签收 |
| MOB-172 / #527 | `hct_reminder/计划提醒` 通道真实入栏；短延迟通知可点击回到应用，载荷仅为“家健镜提醒/请打开应用查看今日安排。”，系统记录 `vis=PRIVATE` | 短延迟 smoke 通过，完整六状态/锁屏矩阵待签收；详见 [短延迟通知记录](MOB-172-Android短延迟通知走查记录-20260901.md) |
| MOB-173 / #528 | 提醒列表和详情显示服务端风险审计、规则/预算/合并等字段；缺失字段显示安全降级文案 | 演示字段通过，真实受控响应和截图隐私待验证 |
| MOB-174 / #529 | 11 个主要路由横屏无根文档溢出；助手草稿旋转后保持 | 非折叠设备局部通过，折叠屏/逐页截图/TalkBack 待签收 |
| MOB-175 / #530 | 演示回答无 citation 时显示无证据降级；知识详情入口可用 | 无证据降级通过，受控 citation 定位/读屏待验证 |

## 5. 发布阻塞与下一步

以下事项仍不能由本次走查代替：

1. 配置 JDK 21 并由维护者在当前候选提交上重建、安装 APK，记录版本和 SHA-256；特别需要复验 MOB-169、MOB-171 的修复内容。
2. 启动受控 HTTPS 后端和合成数据，完成联网搜索、真实天气、风险审计、知识 403/citation、会话和多家庭链路。
3. 按各矩阵执行前台/后台/锁屏/息屏/划掉/重启、电池优化、自启动、勿扰、弱网/断网恢复、TTS、来电抢占、触觉、TalkBack、特大字号、小屏和 PWA 安装态，并保存脱敏附件哈希。
4. 使用折叠设备或等价受控形态完成内/外屏、铰链安全区和状态保持；维护者完成升级失败、回滚和最终门禁记录。

在上述证据齐全前，MOB-160～MOB-175 只能标记为“代码/演示局部完成”或“待验收”，移动端发布总门禁保持 `BLOCKED`。
