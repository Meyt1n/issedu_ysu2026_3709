# HCT-409 后端发布验收记录

- 日期：2026-08-13
- Issue：[#72](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/72)
- 切片：[HCT-409 后端发布验收](../stories/HCT-409-后端发布验收.md)
- 数据政策：仅合成标识符；报告不含密钥或健康正文
- 本记录不是项目负责人签署，不关闭 #72

## 1. 基础档 API 延迟（in-process TestClient）

本机 Windows / Python 3.11.15，40 次采样、5 次预热，RSS 约 80 MiB。

| 端点 | P50 (ms) | P95 (ms) | 错误率 | 预算 P95 |
|---|---:|---:|---:|---:|
| `GET /health` | 0.624 | 0.989 | 0 | 2000 |
| `GET /api/v1/health/db` | 0.958 | 1.305 | 0 | — |
| `POST /api/v1/households` | 1.848 | 2.575 | 0 | 5000 |
| `GET /api/v1/households` | 2.078 | 2.591 | 0 | — |

已知限制：这不是多机压测，也不是视觉 OCR/条码/融合全流程。全流程 CPU P95 ≤ 8 秒仍受 HCT-201/#48 阻塞。

复现：`uv run python scripts/hct409_api_perf.py`

CI 最新 JSON 制品：`artifacts/hct409-api-perf.json`（14 天）。

## 2. 安全与隐私回归包

机器可读清单：`tests/e2e/hct409_release_pack.json`。覆盖授权/撤权/跨家庭、出口守卫、日志脱敏、无导流、文件上传、本地认证、红队、删除传播和助手降级。

## 3. 依赖与密钥扫描

- 密钥：CI `Secret scan`（gitleaks）
- Python 运行时：`uv run python scripts/hct409_dependency_audit.py`，对照 `docs/reviews/hct409-pip-audit-allowlist.json`
- 前端生产依赖：`npm audit --registry=https://registry.npmjs.org --omit=dev --audit-level=high`

当前允许清单仅登记 FastAPI 0.115 钉死的 starlette 0.46.2 已知 ID；新 ID 必须让门禁失败。

## 4. 风险决定

见 `docs/reviews/hct409-risk-register.json`。视觉全流程 P95、生产身份、人工读屏和 R3 签署为延期；离线灾备改写与 starlette 钉选为接受。
