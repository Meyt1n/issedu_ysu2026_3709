# HCT-105：历史同步归属审计与 dry-run

## 审计范围

- GitHub 仓库：`Meyt1n/issedu_ysu2026_3709`
- 内部 `master` 起点：`b62b8acef2b8173874cd6639a7a5c22d9cadbdae`
- GitHub `master` 目标：`7990b92a21930c69a38b4c2c705ae9fec99ab264`
- 关系：内部起点是 GitHub 目标的祖先；没有分叉，不需要强制覆盖。
- 结果：第一父链 10 个节点，按提交身份拆分后 30 个 dry-run 动作。

本审计不修改 GitHub 历史，不修改内部仓库，不重写 Git author，也不使用任何 Token 推送。

## PR #141 混合提交

PR #141 的作者是 `Meyt1n`，但 PR 内唯一业务提交为：

| 提交 | GitHub 关联作者 | 内部 Token | 动作 |
|---|---|---|---|
| `a8fdd9725adb` | `ry12-20` | `CLOUD_TOKEN_RY12_20` | 先同步 PR 提交 |
| `a885bb68fda7` | `Meyt1n` | `CLOUD_REPO_PASSWORD` | 再同步 PR merge 提交 |

这样保留原始 SHA 和 Git 历史，不把 `ry12-20` 的业务提交归入 `Meyt1n`。PR #141 的混合作者问题只能在历史同步层按提交拆分处理；不能通过重写 author 或重新提交来修正。

## 两个直接提交/回退提交

| 提交 | 类型 | 作者 | 内部 Token |
|---|---|---|---|
| `ef6abf943956` | 直接提交：更新 React 前端和路由测试 | `ry12-20` | `CLOUD_TOKEN_RY12_20` |
| `f0d574c433556` | 直接回退上一提交 | `ry12-20` | `CLOUD_TOKEN_RY12_20` |

它们没有关联 PR，但作者身份由 GitHub 关联账号和已登记提交身份确认，dry-run 将它们作为两个独立的单提交动作，不借用维护者 Token。

## 完整 dry-run 动作摘要

| 顺序 | 节点 | 归属 |
|---:|---|---|
| 1–2 | PR #141：`a8fdd9725adb`、`a885bb68fda7` | `ry12-20` → `Meyt1n` |
| 3–4 | PR #143 | `Shen-huang-123` |
| 5–7 | PR #144 | `Shen-huang-123` |
| 8–9 | `ef6abf943956`、`f0d574c433556` | `ry12-20` |
| 10–11 | PR #145 | `Shen-huang-123` |
| 12–13 | PR #146 | `ry12-20` |
| 14–15 | PR #147：提交、merge | `ry12-20` → `Meyt1n` |
| 16–17 | PR #148：提交、merge | `ry12-20` → `Meyt1n` |
| 18–30 | PR #149：12 个 PR 提交、merge | `ry12-20` → `Meyt1n` |

计划器在每个动作前后要求连续快进关系；每个 PR 的第二父链必须与 GitHub PR 提交清单完全一致；未知作者、混合提交、缺失元数据或历史分叉都会停止计划生成。

## 可复现入口

仓库新增手动工作流：`.github/workflows/sync-cloud-history-dry-run.yml`。

它只读取内部 `master`、调用 GitHub API 获取已公开的 PR/提交元数据、生成 JSON 计划并上传 Artifact，明确不会向内部仓库 push。只有 dry-run 计划经过负责人确认后，才允许另行设计一次性执行方案；当前不宣称内部仓库已经追平。

## 当前结论

- PR #141 的混合提交已经定位到 `a8fdd9725adb`。
- 两个无 PR 的直接提交/回退已经定位并可按 `ry12-20` 归属。
- 不需要把整个历史统一使用 `Meyt1n` Token。
- 当前只完成审计和 dry-run 计划，尚未修正内部平台的历史统计，也尚未向内部仓库 push。
