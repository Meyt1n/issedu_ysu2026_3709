# HCT-402 离线盲测评估增量记录

- 评审对象：HCT-402 / Issue #65
- 本增量：数据准备之后的离线盲测评估器和合成回放演示
- 负责人：Meyt1n
- 复核人：Shen-huang-123
- 分支：`codex/hct-402-blind-eval`
- 基线：GitHub `master` `1b62357`

## 交付内容

1. `scripts/hct402_evaluate_blind.py`：只读取盲测输入、标签和结构化预测 JSONL，不调用网络或模型；
2. 预测集合完整性检查：重复、缺失和多余 `sample_id` 直接失败；
3. 指标：结构化格式成功率、状态准确率、路由准确率、引用有效率/召回率、安全拒答率和未授权字段泄露率；
4. 预测、标签和输入 SHA-256 写入报告；
5. `synthetic-evaluator-replay.jsonl`：仅用于复现评估器的合成回放 fixture。

## 可复现结果

命令：

```powershell
uv run ruff check scripts/hct402_prepare_dataset.py scripts/hct402_evaluate_blind.py tests/data/test_hct402_dataset.py tests/data/test_hct402_blind_eval.py
uv run pytest tests/data/test_hct402_dataset.py tests/data/test_hct402_blind_eval.py
uv run python scripts/hct402_prepare_dataset.py --source tests/fixtures/hct402/starter_source.jsonl --output-dir tmp/hct402-blind-eval-demo/prepared
uv run python scripts/hct402_evaluate_blind.py --inputs tmp/hct402-blind-eval-demo/prepared/blind/inputs.jsonl --labels tmp/hct402-blind-eval-demo/prepared/blind/labels.jsonl --predictions tests/fixtures/hct402/synthetic-evaluator-replay.jsonl --model-name synthetic-evaluator-replay --model-version fixture-v1 --output tmp/hct402-blind-eval-demo/report.json
```

结果：Ruff 通过，`8 passed`；合成回放为 4 条样本，格式、状态、路由、引用和安全拒答均为 `1.0`，未授权字段泄露率为 `0.0`。报告的 `evaluation_scope` 为 `synthetic_fixture_only`。

全量回归：`429 passed, 19 skipped, 2 failed`。两个失败均为 master 既有同步基线：内部云端 `master` 为 `1251d074...` 而 GitHub `master` 为 `1b62357...`，以及历史同步记录作者校验期望 `zhang` 但当前作者为 `Wind`；不属于本 PR 变更范围。

PR #124 首次 CI 的 Secret scan 失败不是泄露命中，而是 Gitleaks v2 在复杂合并历史中把 PR 提交列表首项当作不可用 base，`git log` 因此返回 `ambiguous argument`。本增量在 `.github/workflows/ci.yml` 显式传入 GitHub 事件的 `pull_request.base.sha` 作为 `BASE_REF`，不改变扫描规则、完整历史或失败门禁。

## 边界与未完成项

- 该结果只证明评估器和数据协议可运行，不证明任何基础模型或 QLoRA 模型质量；
- 没有提交真实健康对话、模型权重、训练缓存或盲测答案以外的敏感数据；
- HCT-402 仍未完成 QLoRA 训练、基础模型对照、真实盲测、人工抽检、模型卡和发布/回滚评审；
- 未达到门槛时继续使用基础模型或结构化降级，不将合成回放标为已发布模型。
