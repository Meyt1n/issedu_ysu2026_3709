# HCT-401 权威健康知识与白名单扩充验收（2026-08-29）

## 关联与范围

- Issue：[#613](https://github.com/Meyt1n/issedu_ysu2026_3709/issues/613)
- Story：[HCT-401](../stories/HCT-401-知识入库权限前置过滤与RAG引用校验.md)
- FR/NFR：FR-08、NFR-02、NFR-03、NFR-04、NFR-07
- 风险：R3；知识来源和医疗安全边界变更必须保留人工审核、权限过滤与回滚证据。
- 允许修改：`docs/knowledge/crawl/allowlist.json`、HCT-401 Story/追踪/评审记录、对应单元测试。

## 结果

- `allowed_hosts`：27 个官方 HTTPS 主机。
- 来源清单：27 条，其中 6 个离线教学夹具、21 个远程来源登记；本次新增远程来源全部 `enabled: false`。
- 安全默认：`https_only=true`、`auto_ingest=false`、`requires_human_review=true`。
- 来源覆盖：国家卫生健康委、中国疾控、WHO、CDC、FDA、DailyMed、MedlinePlus、NIA、NIDDK、NHLBI、AHRQ 等官方站点。
- 每条远程来源登记 `license=public-page-summary-only`、刷新周期、主题及医疗边界说明；禁止整页版权复制、诊断、剂量、停换药和购药/问诊导流。

## 本机正式账号验收

- 账号：`demo-parent`；运行时批准批次新增 100 个独立中文摘要文档、100 个唯一标题和正文 SHA-256。
- 索引：`approved-web-100-20260829-v1`，索引 ID `22f6330e-ff34-4deb-8cdc-a7518bb05324`。
- 全库：108 个有效文档、532 个检索分块；校验和 `b76a0f767a43b8ab75ab140708875f8d09d1f06aabcf3b4462a235b56a636aef`。
- 幂等：首次 dry-run 为 100 `create`；正式原子入库成功；重复 dry-run 为 100 `skip` / 0 `create`。
- 权限：`demo-parent` 可见 108 个文档；`unrelated-viewer` 可见 0 个文档，检索结构化降级为 `NO_AUTHORISED_DOCUMENTS`。
- 主题检索：用药、血压、糖尿病、跌倒、呼吸道、饮食、睡眠、心理、应急、最小必要共享共 10 组查询全部返回结果且未降级。
- 真实搜索：受控 DuckDuckGo 查询成功返回 5 条参考，前三条来自国家卫健委/中国政府；未知域名和 HTTP URL 仍由出口守卫阻断。

> 100 条正文与批准清单是本机 gitignore 运行产物，不进入仓库；仓库只提交可长期维护的来源白名单、来源元数据、测试和无隐私验收摘要。数据库文档仍受 HCT-401 权限与删除传播约束。

## 自动化证据

- `uv run pytest tests/unit/test_local_knowledge_ingest.py tests/unit/test_knowledge_crawl.py tests/unit/test_health_news.py tests/unit/test_hct430_search_providers.py tests/unit/test_hct401_knowledge_gold.py tests/safety/test_hct445_health_news_adapter.py tests/safety/test_hct430_open_egress.py -q`
- 修改前定向回归：69 项通过；本 PR 新增权威来源安全默认断言并基于最新 `origin/master` 重跑后共 72 项通过。
- 运行服务：API、MySQL、Web、outbox worker、care-plan worker 健康；本机 Ollama 的配置模型可见。服务健康只证明依赖就绪，不代表临床审核完成。

## 回滚

1. 回退本 PR 的 allowlist/source 登记提交；远程源本来即默认关闭，不影响已批准索引。
2. 紧急关闭联网：`AGENT_WEB_SEARCH_ENABLED=false`，健康资讯设 `HEALTH_NEWS_ADAPTER=local`，重启 API。
3. 知识回滚：将本批 100 个文档标记删除后创建新索引快照；保留旧索引审计记录。
4. 禁止通过开启 `auto_ingest`、关闭权限过滤或改用云端健康上下文规避故障。
