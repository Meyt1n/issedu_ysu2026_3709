# 多模态医疗项目协作规范

本规范用于项目启动和后续研发。所有参与者同时遵守
[Vibe Coding 开发约束](docs/vibe-coding/03-Vibe-Coding开发约束.md)。

## 1. 领取任务

开始编码前，Issue 必须包含：

- 对应需求编号，例如 `FR-02`、`NFR-05`；
- 背景、范围与明确不做的内容；
- 可验证的验收条件；
- 隐私、权限、医疗安全和模型风险；
- 负责人、评审人和预计交付时间。

没有需求编号或验收条件的任务只允许做探索，不得直接合并产品代码。

## 2. 分支与提交

本仓库默认分支为 `master`。从最新 `master` 建分支：

```bash
git switch master
git pull --ff-only origin master
git switch -c feature/用户名-任务简述
```

允许的分支类型：

```text
feature/用户名-功能
fix/用户名-问题
docs/用户名-文档
test/用户名-测试
refactor/用户名-范围
chore/用户名-事项
experiment/用户名-实验
```

提交信息采用：

```text
feat: 添加药物图片上传校验
fix: 阻止跨用户知识库检索
test: 增加高风险症状升级回归
docs: 更新数据删除流程
```

一次提交只包含一个可解释的目标。模型权重、数据集、`.env`、密钥、日志和本地缓存不得提交。

## 3. 开发流程

1. 阅读需求、架构、接口和对应模块代码。
2. 先更新契约/设计，再做数据库、公开 API、权限或模型变更。
3. 优先完成最小纵向链路，不提前实现未进入当前里程碑的功能。
4. 新逻辑补单元测试；跨服务链路补集成测试；医疗安全规则补 `tests/safety/` 回归。
5. 本地运行格式、静态检查和相关测试。
6. 人工检查完整差异后推送并创建 PR。

## 4. Pull Request 门禁

PR 必须：

- 关联 Issue 和需求编号；
- 说明实现、影响、测试证据、已知限制和回滚方式；
- 声明 AI 参与范围和人工复核内容；
- 不混入无关重构；
- 通过 CI；
- 至少一名组员评审。

认证、权限、加密、医疗安全、数据删除、模型发布或数据库迁移需要两人评审。

## 5. 工程检查

当前项目尚未建立代码检查命令。文档 PR 至少执行：

```powershell
git diff --check
```

首个工程增量应建立本地与 CI 一致的格式、静态、单元和集成检查。后续前端建立后必须执行类型检查、单元测试和构建。无法运行某项检查时，应在 PR 中写明原因，不能写成“已通过”。

## 6. 实验与产品代码

- 实验放在独立分支和明确目录，记录数据、参数、指标与结论。
- 实验 notebook 不能作为唯一实现，进入产品前需转为可测试模块。
- 模型或 RAG 策略达到登记基线并通过安全回归后才可成为默认版本。
- 未通过的实验可以保留报告，但不得伪装成已交付功能。

## 7. 合并与发布

- 禁止直接向 `master` 推送和对共享分支强制推送。
- 使用 squash 或保留清晰提交历史；合并后删除远程任务分支。
- 需求、API、数据结构、模型、提示词或部署行为变化必须同步文档。
- 发布必须绑定代码、数据库、模型、知识库和提示词版本。
