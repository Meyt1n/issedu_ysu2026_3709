# 双仓库同步提交说明（已废止）

本文件记录的是 2026-08-11 之前的“双仓库自动同步”方案，现已废止。GitHub 与内部仓库不再关联，不能按本文件配置远端、Token、Actions 或双推流程。

当前唯一有效的操作说明是：[双仓库解耦与独立克隆说明](docs/vibe-coding/双仓库解耦与独立克隆说明.md)。它规定了：

- GitHub `master` 作为 HomeCare Twin 唯一开发和合并基线；
- 内部仓库一次性回退到根提交后独立保留；
- 两个仓库分别 clone，不互加远端、不互相 push；
- 删除旧的内部仓库同步 workflow、身份映射脚本、活动测试和不再使用的 GitHub Secrets；
- 旧同步记录只用于历史审计，不代表当前完成状态。

请勿从 Git 历史恢复旧的同步 workflow。若未来确需重新关联，必须先建立新的架构决策并完成凭据、统计归属、分叉处理、回滚和隐私风险评审。
