# 双仓库同步提交说明

本项目同时维护在原云端仓库和 GitHub 仓库中：

- 原云端仓库：`http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git`
- GitHub 仓库：`https://github.com/Meyt1n/issedu_ysu2026_3709.git`
- 主分支：`master`

## 日常提交与同步

已经完成双推配置的电脑，每次提交时按以下顺序操作：

```bash
# 1. 从原云端拉取最新代码
git pull --rebase origin master

# 2. 查看本次改动
git status

# 3. 只暂存需要提交的文件
git add <文件或目录>

# 4. 创建提交
git commit -m "简要说明本次修改"

# 5. 同时推送到原云端和 GitHub
git push origin master
```

当前项目已为 `origin` 配置两个推送地址，因此最后一条命令会依次推送到两个仓库。

推送结束后应检查输出，确认两个地址都显示推送成功。双仓库推送不是原子操作：如果网络、权限或远端历史存在问题，可能出现一个仓库成功、另一个仓库失败。修复问题后应重新执行推送。

## GitHub PR 合并后自动同步

仓库通过 [`.github/workflows/sync-original-cloud.yml`](.github/workflows/sync-original-cloud.yml) 自动同步 GitHub `master` 到原云端：

1. GitHub PR 合并进入 `master`，或有人直接推送 GitHub `master`。
2. GitHub Actions 获取原云端 `master`。
3. 确认原云端提交是 GitHub 当前提交的祖先后，执行快进推送。
4. 如果两端历史已分叉，工作流会失败并停止，不会强制覆盖任一仓库。

仓库管理员必须在 GitHub 仓库的 `Settings → Secrets and variables → Actions` 中配置以下 Repository secrets：

- `CLOUD_REPO_USERNAME`：原云端仓库用户名。
- `CLOUD_REPO_PASSWORD`：原云端仓库密码或访问令牌。

该自动化以 GitHub 为 PR 和合并入口，方向为 **GitHub → 原云端**。不要绕过 GitHub 直接向原云端 `master` 提交新历史；否则两端可能分叉，需要人工合并。工作流也支持在 GitHub Actions 页面手动执行。

### 队友配置 GitHub PR 自动同步

#### 先确认是否需要重复配置

- 如果所有队友都向同一个 GitHub 仓库提交 PR：仓库管理员只需配置一次工作流和 Repository secrets。工作流合并到 `master` 后，所有队友的 PR 都会自动同步，普通队友不需要重复上传原云端凭据。
- 如果队友维护的是独立 GitHub 仓库或 Fork：每个仓库都需要由各自管理员重复下面的配置，并把工作流中的原云端地址改成该仓库对应的地址。

建议使用专门的同步账号或访问令牌，只授予目标原云端仓库的写入权限。不要在工作流、提交记录、PR 描述或聊天消息中填写明文密码。

#### 队友 Token 与提交身份

同一个 GitHub 仓库只需要一组仓库级原云端密钥。各成员的 Token 分工如下：

- 每位队友使用自己的 GitHub Token 登录 GitHub、推送个人分支和创建 PR。
- PR 合并进入 GitHub `master` 后，工作流统一使用 `CLOUD_REPO_USERNAME` 和 `CLOUD_REPO_PASSWORD` 推送原云端。
- 队友不需要把个人 GitHub Token 或个人原云端 Token 交给仓库管理员，也不需要为同一仓库重复创建同名 Repository secrets。
- 即使 PR 来自 Fork，自动同步也在合并后的 GitHub `master` `push` 事件中执行，不会向 Fork 或 PR 分支暴露仓库密钥。

原云端接收到的是完整 Git 提交历史，而不是一份新的 PR：

- Git 提交中的作者姓名和邮箱会保留，可以通过 `git log --format=fuller` 查看贡献者。
- 使用普通合并时，合并提交消息通常会保留 GitHub PR 编号和标题。
- GitHub PR 的评论、审核意见、检查记录、标签和网页不会复制到原云端。
- 原云端的“谁执行了推送”审计通常显示统一同步账号，而不是 PR 作者；PR 作者身份以 Git 提交元数据和 GitHub PR 页面为准。

为了正确记录每位成员的贡献，队友首次提交前应配置自己的真实姓名和邮箱：

```bash
git config user.name "队友姓名或 GitHub 用户名"
git config user.email "队友在 GitHub 验证过的邮箱"
```

如果项目要求原云端的推送审计也必须显示每位成员本人，就不能使用统一的自动同步账号，需要每位成员使用自己的原云端凭据手动双推，或额外建设支持身份映射的服务端集成。

#### 第一步：确认两端历史可以安全快进

在本地仓库中获取两个远端的 `master`：

```bash
git fetch https://github.com/<GitHub用户名>/<GitHub仓库名>.git \
  master:refs/remotes/github-sync/master
git fetch http://<原云端地址>/<原云端仓库路径>.git \
  master:refs/remotes/cloud-sync/master

git merge-base --is-ancestor \
  refs/remotes/cloud-sync/master \
  refs/remotes/github-sync/master
```

最后一条命令退出码为 `0`，表示原云端可以安全快进到 GitHub；可以继续配置。若退出码不为 `0`，说明两端已经分叉，应先人工检查并合并历史，不能使用 `--force` 覆盖。

#### 第二步：添加同步工作流

1. 将本仓库的 [`.github/workflows/sync-original-cloud.yml`](.github/workflows/sync-original-cloud.yml) 复制到目标 GitHub 仓库的相同路径。
2. 修改工作流中的 `CLOUD_REPO_URL`，使其指向正确的原云端仓库。
3. 如果目标仓库的主分支不是 `master`，同时修改 `on.push.branches`、远端获取和推送命令中的分支名。
4. 保留快进检查，不要把推送命令改成 `git push --force`。

#### 第三步：配置 GitHub Repository secrets

需要 GitHub 仓库管理员权限。可以在网页进入：

`Settings → Secrets and variables → Actions → New repository secret`

依次创建：

- `CLOUD_REPO_USERNAME`
- `CLOUD_REPO_PASSWORD`

也可以使用已登录的 GitHub CLI。以下命令会交互式读取密钥值，不会把值写入命令历史：

```bash
gh auth status
gh secret set CLOUD_REPO_USERNAME --repo <GitHub用户名>/<GitHub仓库名>
gh secret set CLOUD_REPO_PASSWORD --repo <GitHub用户名>/<GitHub仓库名>
gh secret list --repo <GitHub用户名>/<GitHub仓库名>
```

`gh secret list` 只会显示密钥名称和更新时间，不会显示密钥内容。

#### 第四步：通过 PR 发布工作流

```bash
git switch -c chore/github-pr-auto-sync
git add .github/workflows/sync-original-cloud.yml 双仓库同步提交说明.md
git diff --cached --check
git commit -m "chore: 增加 GitHub PR 自动同步"
git push -u github chore/github-pr-auto-sync
```

然后在 GitHub 创建 PR，目标分支选择 `master`。PR 合并产生的 `master` 推送会触发新工作流，并将该合并提交同步到原云端。

#### 第五步：检查首次运行

1. 打开 GitHub 仓库的 `Actions` 页面。
2. 进入“同步 master 到原云端仓库”工作流。
3. 确认最新一次运行结论为绿色的 `Success`。
4. 分别查询两个远端的 `master`：

```bash
git ls-remote \
  https://github.com/<GitHub用户名>/<GitHub仓库名>.git \
  refs/heads/master
git ls-remote \
  http://<原云端地址>/<原云端仓库路径>.git \
  refs/heads/master
```

两条命令输出的提交哈希完全相同，表示自动同步成功。

需要重新测试时，可在 Actions 页面点击 `Run workflow`，也可以使用：

```bash
gh workflow run sync-original-cloud.yml \
  --repo <GitHub用户名>/<GitHub仓库名> \
  --ref master
```

#### 常见故障

- 提示缺少 `CLOUD_REPO_USERNAME` 或 `CLOUD_REPO_PASSWORD`：检查密钥名称是否完全一致，并确认密钥配置在 Repository secrets 而不是环境密钥中。
- 返回 `401` 或 `403`：原云端账号、密码或令牌无效，或者同步账号没有目标仓库写入权限。
- 提示两端历史已分叉：停止自动同步，分别拉取两个远端并人工合并；不要修改工作流使用强制推送。
- 无法连接原云端：确认原云端允许 GitHub 托管运行器访问其地址和端口，并检查防火墙或访问白名单。
- PR 合并后没有运行记录：确认工作流已经存在于默认分支、监听的分支名正确，并检查仓库是否禁用了 GitHub Actions。

## 队友双端同步验收示例

下面以 GitHub 用户 `ry12-20` 为例。其他队友测试时，应把示例中的 `ry12-20` 替换成自己的 GitHub 用户名。每人使用独立文件，可以避免多人测试时互相覆盖。

测试前必须满足以下条件：

1. 已接受 GitHub 新仓库的协作者邀请。
2. 已拥有原云端仓库的写入权限。
3. 已按照“新电脑或新成员首次配置”完成两个推送地址的配置。
4. 执行 `git remote get-url --all --push origin` 时能看到原云端和 GitHub 两个地址。

### 第一步：拉取最新代码

```bash
git switch master
git pull --rebase origin master
```

### 第二步：创建个人测试文件

在项目根目录下新建文件：

```text
sync-test/ry12-20.md
```

文件内容示例：

```markdown
# 双端同步测试

- 测试人：ry12-20
- 测试内容：验证一次提交能否同时推送到原云端和 GitHub
- 测试结果：等待推送后填写
```

### 第三步：提交测试文件

```bash
git status
git add sync-test/ry12-20.md
git diff --cached --check
git commit -m "test: ry12-20 验证双端仓库同步"
```

`git status` 中不应包含其他人的文件或与本次测试无关的改动。如果存在无关改动，不要使用 `git add .` 或 `git add -A`。

### 第四步：一次推送到两个仓库

```bash
git push origin master
```

成功时，终端输出中应该分别出现原云端地址和 GitHub 地址，并且两处都没有 `rejected`、`403` 或其他错误。

### 第五步：核对两个远端

```bash
git fetch origin
git fetch github
git rev-parse origin/master
git rev-parse github/master
git log -1 --oneline origin/master
git log -1 --oneline github/master
```

验收通过需要同时满足：

- 两条 `rev-parse` 命令输出完全相同的提交哈希。
- 两条 `git log` 命令都显示 `test: ry12-20 验证双端仓库同步`。
- 在原云端仓库和 GitHub 仓库的 `master` 分支中都能看到 `sync-test/ry12-20.md`。

如果只有一个仓库更新，不要重新创建提交。先根据终端错误修复权限、网络或远程配置，再重新执行：

```bash
git push origin master
```

常见问题：

- GitHub 返回 `403`：确认已接受 GitHub 协作者邀请，并确认当前使用的是受邀账号。
- 原云端返回 `401` 或 `403`：确认该成员拥有原云端写入权限，并重新登录或更新凭据。
- 推送提示 `fetch first`：先执行 `git pull --rebase origin master`，处理完冲突后再推送。
- 推送输出中只有一个仓库地址：该电脑尚未正确配置两个 `origin` 推送地址。

## 新电脑或新成员首次配置

双推地址保存在每台电脑本地的 `.git/config` 中，不会随着 Git 提交或克隆操作自动同步。通过 GitHub 克隆项目后，需要在项目目录中执行以下配置：

```bash
# 将克隆时自动生成的 origin 改名为 github
git remote rename origin github

# 添加原云端作为主要拉取源
git remote add origin http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git

# 为 origin 设置两个推送目标
git remote set-url --add --push origin http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git
git remote set-url --add --push origin https://github.com/Meyt1n/issedu_ysu2026_3709.git

# 让本地 master 默认跟踪原云端 master
git branch --set-upstream-to=origin/master master
```

完成后可用下面的命令确认 `origin` 有两个推送地址：

```bash
git remote get-url --all --push origin
```

成员必须同时拥有两个仓库的写入权限。原云端如果要求用户名或访问令牌，请使用 Git Credential Manager、系统钥匙串或 SSH 保存凭据，不要把令牌直接写入远程 URL，也不要把令牌提交到仓库。

## 只同步某一个仓库

需要单独推送到 GitHub 时：

```bash
git push github master
```

需要检查两个远端是否已同步时：

```bash
git fetch origin
git fetch github
git rev-parse origin/master
git rev-parse github/master
```

两条 `rev-parse` 命令输出的提交哈希一致，表示两个远端的 `master` 分支处于同一版本。

## 冲突与推送失败

如果提示远端包含本地没有的提交，不要使用普通的 `--force` 覆盖远端。先执行：

```bash
git pull --rebase origin master
```

解决冲突并完成变基后，再执行：

```bash
git push origin master
```

如果某个仓库已经被其他成员单独更新，应先分别获取两个远端并检查历史，确认差异后再合并，避免丢失任何一端的提交。
