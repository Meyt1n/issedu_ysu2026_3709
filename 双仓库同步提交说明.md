# 双仓库同步提交说明

本项目同时保留 GitHub 仓库和内部云端仓库，但开发与合并只以 GitHub 为准：

- GitHub：`https://github.com/Meyt1n/issedu_ysu2026_3709.git`，负责 Issue、PR、Review、CI 和 `master` 基线。
- 内部云端：`http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git`，只接收 GitHub `master` 的自动镜像。
- 主分支：`master`。

谁创建 PR、谁提交代码，Git author 信息仍归谁。同步工作流只复制同一个 Git 提交对象，不重新提交、不改写提交作者；如果内部云端按 Token 统计，推送记录会归到本次使用的 Token 账号，因此工作流会优先按合并 PR 作者选择已配置的云端 Token。

## 1. 核心规则

1. 所有开发从 GitHub `master` 开始，所有功能通过 GitHub PR 合并。
2. 普通成员只向 GitHub 推送个人分支，禁止向 GitHub 或内部云端直接推送 `master`。
3. 禁止同时向两个仓库推送同一分支，禁止在内部云端重新提交一份相同修改。
4. GitHub `master` 合并后由 Actions 自动同步到内部云端 `master`。
5. 发现两端提交 SHA 不一致时立即停止推送，交给仓库管理员处理；不得自行使用裸 `--force`。

内部仓库原有的贡献统计方式继续作为统计依据，本文件不新增统计脚本或统计口径。Git author 由成员本地提交身份决定；Token 统计则由同步工作流的作者映射决定。功能 PR 没有成员映射、提交账号与 PR 作者不一致或 Secret 缺失时，同步必须失败，禁止回退到其他成员 Token。

## 2. 成员远端配置

建议每台电脑使用两个远端名称：`github` 用于拉取和推送，`cloud` 用于核对内部云端。旧配置如果让 `origin` 同时拥有两个 `pushurl`，必须先清理，不能继续双推。

先确认没有未保存改动：

```bash
git status --short --branch
git remote -v
git remote get-url --all --push origin
```

如果当前远端名是 `origin`，按下面方式整理为 GitHub 单一推送源：

```bash
git config --unset-all remote.origin.pushurl
git remote set-url origin https://github.com/Meyt1n/issedu_ysu2026_3709.git
git remote rename origin github
git remote add cloud http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git
```

如果本地没有 `origin`，直接添加：

```bash
git remote add github https://github.com/Meyt1n/issedu_ysu2026_3709.git
git remote add cloud http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git
```

配置完成后应满足：

- `github` 指向 GitHub，并且日常 `git push` 只使用 `github`；
- `cloud` 指向内部云端，只用于检查同步结果；
- 不再存在两个 `pushurl`；
- 不使用 `git push origin master`，不使用双推脚本。

## 3. 每次开发与提交流程

### 3.1 从 GitHub `master` 创建分支

```bash
git fetch github master
git switch master
git pull --ff-only github master
git switch -c feature/<姓名>-<任务简称>
```

开发前必须阅读 `AGENTS.md` 指定的入口文档，并在 GitHub Issue、Story 和 PR 中填写任务范围、FR/NFR、验收条件、风险、测试和回滚方式。没有 Issue 的会议记录、维护性或纯资料 PR 可以跳过 Relay Review Bot，但仍受 CI、Secret Scan 和合并人核对约束。

### 3.2 提交并推送个人分支

先确认提交身份是本人：

```bash
git config user.name "你的姓名或 GitHub 用户名"
git config user.email "你在 GitHub 验证过的邮箱"
git config --get user.name
git config --get user.email
```

只暂存本次任务的文件，禁止把他人改动、健康数据、密钥、缓存或模型文件混入：

```bash
git status --short
git diff --check
git add <明确的文件或目录>
git diff --cached --check
git commit -m "类型: 简要说明变更"
git show -s --format="commit=%H%nauthor=%an <%ae>%ncommitter=%cn <%ce>%nsubject=%s" HEAD
git push -u github feature/<姓名>-<任务简称>
```

然后在 GitHub 创建目标为 `master` 的 PR。PR 合并时应保留正常 Git 提交历史；不要在内部云端再创建一个“对应提交”。

### 3.3 合并前检查

当前 `master` 的 Required Checks 为：

- `Mandatory development docs`；
- `Backend lint, migration and tests`；
- `Frontend typecheck and build`；
- `Secret scan`。

`Task association and risk metadata` 自动检查已移除，不会再为新 PR 生成状态。Relay Review Bot 仍会运行并报告结果，但当前不是 Required Check；没有 Issue 的会议记录、维护性或纯资料 PR 会由 Relay Review Bot 白名单跳过，不调用中转 API。功能 PR 仍应绑定 Issue、Story 和 FR/NFR，并由合并人在 merge 前核对；维护者 merge 即代表本仓库流程中的人工复核完成，不额外要求 approval。

## 4. GitHub 合并后自动同步

工作流 [`.github/workflows/sync-original-cloud.yml`](.github/workflows/sync-original-cloud.yml) 监听 GitHub `master` 的推送，流程如下：

1. PR 合并进入 GitHub `master`。
2. Actions 使用维护者凭据只读获取内部 `master`，计算内部 SHA 到本次 GitHub SHA 之间的第一父链提交。
3. 每个待同步提交必须关联唯一的、已合并到 `master` 的 PR；工作流读取 PR 作者和 PR 内全部提交账号。
4. [身份解析脚本](.github/scripts/cloud_sync_identity.py)确认 PR 作者与提交账号属于同一成员，再返回该成员的内部用户名和 Token Secret 名称。
5. 工作流在任何 push 前预检整批待同步 PR；未知作者、混合提交账号或缺少 Secret 时直接失败，不使用其他成员 Token。
6. 预检通过后，工作流按 GitHub 第一父链顺序逐个 PR 快进内部 `master`，每次 push 使用该 PR 成员自己的 Token。
7. 每次 push 后立即核对内部 SHA，最终两端 SHA 必须一致；历史分叉时失败并停止，不强制覆盖。

管理员需要在 GitHub `Settings → Secrets and variables → Actions` 中配置：

- `CLOUD_REPO_USERNAME`：GitHub 用户 `Meyt1n` 对应的内部云端用户名，同时用于读取内部 `master`；
- `CLOUD_REPO_PASSWORD`：GitHub 用户 `Meyt1n` 对应的内部云端 Token，同时用于只读获取内部 `master`；
- `CLOUD_TOKEN_SHEN_HUANG_123`：GitHub 用户 `Shen-huang-123` 对应的内部云端 Token，Token 所属统计账号为 `zhang`。

当前映射如下：

| GitHub PR 作者 | 允许的提交身份 | 内部账号 | Token Secret |
|---|---|---|---|
| `Meyt1n` | GitHub `Meyt1n`，或已登记的 Meyt1n Git 姓名/邮箱 | `CLOUD_REPO_USERNAME` | `CLOUD_REPO_PASSWORD` |
| `Shen-huang-123` | GitHub `Shen-huang-123`，或 `zhang <z85963541@qq.com>` | `zhang` | `CLOUD_TOKEN_SHEN_HUANG_123` |

新增成员时，按 `CLOUD_TOKEN_<规范化 GitHub 用户名>` 建立独立 Repository Secret，并同步更新 `.github/scripts/cloud_sync_identity.py` 的 GitHub 登录名、Git 姓名/邮箱、内部用户名和单元测试。Secrets 只能保存在 GitHub Repository secrets 中，不能写入代码、PR、日志、远程 URL 或聊天消息。成员不需要把内部云端凭据安装到个人电脑或发给 agent。

一个 PR 只能归属一个 Token。若 PR 中包含多名成员的提交，合并前应拆分 PR；Git 协议无法让同一次 master push 同时归属多个 Token。

## 5. 同步验收

PR 合并后，在 Actions 页面确认“同步 master 到原云端仓库”运行成功，然后核对两个远端的 `master`：

```bash
git ls-remote https://github.com/Meyt1n/issedu_ysu2026_3709.git refs/heads/master
git ls-remote http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git refs/heads/master
```

两条命令输出的提交 SHA 必须完全相同。若只有一个仓库更新，不要重新创建提交，也不要手动双推；先查看 Actions 日志和远端权限，再由管理员重新运行同步工作流。

同步后，内部云端看到的是 GitHub 合并后的同一份提交历史。成员的姓名、邮箱和提交作者信息保持原样；统计系统如果只按 Token 归属，则一个 PR 的合并 push 会归到该 PR 成员对应的内部账号。Actions 日志应显示 PR 编号、GitHub 作者、提交数量和 Secret 名称，但不得显示 Token 值。

PR24、PR28 等已经使用旧凭据完成的历史记录不能靠重新 push 同一 SHA 改写；成员映射只从本工作流合并后的新同步开始生效。

## 6. 两端分叉时的处理

普通成员发现分叉只报告，不处理。管理员先保存 GitHub SHA、内部云端 SHA 和分叉原因；确认 GitHub 是权威、内部云端没有需要保留的独立提交后，才可以使用带旧 SHA 保护的 `--force-with-lease`。禁止使用裸 `--force`：

```bash
git ls-remote https://github.com/Meyt1n/issedu_ysu2026_3709.git refs/heads/master
git ls-remote http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git refs/heads/master
git fetch github master
git tag backup/cloud-master-before-recovery-<日期> <旧内部云端SHA>
git push --force-with-lease=refs/heads/master:<旧内部云端SHA> http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git <GitHub权威SHA>:refs/heads/master
```

恢复后必须再次核对两端 SHA，并确认提交作者信息没有被重写。如果内部云端存在应保留的独立提交，先备份和人工合并，不能直接覆盖。

## 7. 分支生命周期

- PR 合并后，维护者删除对应的 GitHub 远程分支；
- 已关闭且明确为一次性测试的分支，在确认没有待保留提交后可以删除；
- `master`、当前开发分支和仍有未合并 PR 的分支不得删除；
- 删除分支前先确认 PR 状态和分支 SHA，不用删除分支掩盖未完成任务。

GitHub CLI 示例：

```bash
gh pr view <PR编号> --repo Meyt1n/issedu_ysu2026_3709
gh api --method DELETE repos/Meyt1n/issedu_ysu2026_3709/git/refs/heads/<分支名>
```

## 8. 最终原则

GitHub 负责开发、Review 和合并；内部云端按 PR 成员 Token 自动接收 GitHub `master` 的同一提交。成员只需使用自己的 GitHub 分支和已登记 Git 提交身份完成单一作者 PR，不需要、也不允许再向两个仓库重复提交。
