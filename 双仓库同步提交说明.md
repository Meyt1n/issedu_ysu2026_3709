# 双仓库同步提交说明

本项目同时保留 GitHub 仓库和内部云端仓库，但开发与合并只以 GitHub 为准：

- GitHub：`https://github.com/Meyt1n/issedu_ysu2026_3709.git`，负责 Issue、PR 和 `master` 基线；自动 CI/Relay Review 已按负责人决定停用。
- 内部云端：`http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git`，只接收 GitHub `master` 的自动镜像。
- 主分支：`master`。

## 0. 2026-08-11 一次性恢复与基线同步记录

本次恢复以 GitHub `master` 为唯一权威来源，先备份内部仓库原有恢复点，再对齐内部 `master`：

| 项目 | SHA / 位置 | 说明 |
|---|---|---|
| GitHub 权威 `master` | `341869044fee44a3929e527bc5996f2213a2afc8` | 本次同步源 |
| 内部同步前 `master` | `21cbe549f5faf0f4094c486fc7a7d4d05ee11c3b` | 受保护覆盖前的旧恢复点 |
| 内部恢复备份 | `archive/internal-master-before-github-sync-20260811-21cbe549` | 保留旧恢复点，供回滚或审计 |
| 内部同步后 `master` | `341869044fee44a3929e527bc5996f2213a2afc8` | 已与 GitHub `master` 完全一致 |

本次操作使用 `--force-with-lease` 校验内部旧 SHA 后完成，不改写 GitHub 历史、不改写 Git author，也不重算过去的 Token 统计。它是恢复后的基线对齐，不是给历史提交补造 PR；后续新合并 PR 才按成员映射使用对应 Token。

截至本记录提交时，恢复同步工作流仍在 Draft PR #119 中；在该 PR 合并且 Actions Secrets 重新配置、线上运行验收前，不能宣称“合并后自动同步”已经生效。本次手工基线对齐完成后，后续开发仍只从 GitHub `master` 建分支并通过 PR 合并。

谁创建 PR、谁提交代码，Git author 信息仍归谁。同步工作流只复制同一个 Git 提交对象，不重新提交、不改写提交作者；如果内部云端按 Token 统计，推送记录会归到本次使用的 Token 账号，因此工作流会优先按合并 PR 作者选择已配置的云端 Token。

## 1. 核心规则

1. 所有开发从 GitHub `master` 开始，所有功能通过 GitHub PR 合并。
2. 普通成员只向 GitHub 推送个人分支，禁止向 GitHub 或内部云端直接推送 `master`。
3. 禁止同时向两个仓库推送同一分支，禁止在内部云端重新提交一份相同修改。
4. GitHub `master` 合并后由 Actions 自动同步到内部云端 `master`。
5. 发现两端提交 SHA 不一致时立即停止推送，交给仓库管理员处理；不得自行使用裸 `--force`。

内部仓库原有的贡献统计方式继续作为统计依据，本文件不新增统计脚本或统计口径。Git author 由成员本地提交身份决定；Token 统计则由同步工作流的作者映射决定。功能 PR 没有成员映射、提交账号与 PR 作者不一致或 Secret 缺失时，同步必须失败，禁止回退到其他成员 Token。

## 2. 成员远端配置

建议每台电脑使用两个远端名称：`github` 用于拉取和推送，`cloud` 用于核对内部云端。旧配置如果让 `origin` 同时拥有两个 `pushurl`，必须先清理，不能继续双推。

先确认没有未保存改动：

```bash
git status --short --branch
git remote -v
git remote get-url --all --push origin
```

如果当前远端名是 `origin`，按下面方式整理为 GitHub 单一推送源：

```bash
git config --unset-all remote.origin.pushurl
git remote set-url origin https://github.com/Meyt1n/issedu_ysu2026_3709.git
git remote rename origin github
git remote add cloud http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git
```

如果本地没有 `origin`，直接添加：

```bash
git remote add github https://github.com/Meyt1n/issedu_ysu2026_3709.git
git remote add cloud http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git
```

配置完成后应满足：

- `github` 指向 GitHub，并且日常 `git push` 只使用 `github`；
- `cloud` 指向内部云端，只用于检查同步结果；
- 不再存在两个 `pushurl`；
- 不使用 `git push origin master`，不使用双推脚本。

## 3. 每次开发与提交流程

### 3.1 从 GitHub `master` 创建分支

```bash
git fetch github master
git switch master
git pull --ff-only github master
git switch -c feature/<姓名>-<任务简称>
```

开发前必须阅读 `AGENTS.md` 指定的入口文档，并在 GitHub Issue、Story 和 PR 中填写任务范围、FR/NFR、验收条件、风险、测试和回滚方式。没有 Issue 的会议记录、维护性或纯资料 PR 可以不绑定任务；自动 CI/Relay Review 已停用，合并人必须自行完成人工核对，双仓库同步仍只由合并后的同步工作流执行。

### 3.2 提交并推送个人分支

先确认提交身份是本人：

```bash
git config user.name "你的姓名或 GitHub 用户名"
git config user.email "你在 GitHub 验证过的邮箱"
git config --get user.name
git config --get user.email
```

只暂存本次任务的文件，禁止把他人改动、健康数据、密钥、缓存或模型文件混入：

```bash
git status --short
git diff --check
git add <明确的文件或目录>
git diff --cached --check
git commit -m "类型: 简要说明变更"
git show -s --format="commit=%H%nauthor=%an <%ae>%ncommitter=%cn <%ce>%nsubject=%s" HEAD
git push -u github feature/<姓名>-<任务简称>
```

然后在 GitHub 创建目标为 `master` 的 PR。PR 合并时应保留正常 Git 提交历史；不要在内部云端再创建一个“对应提交”。

### 3.3 合并前检查

当前没有自动 Required Checks。`ci.yml`、`relay-review-bot.yml` 仅保留手动触发入口；PR 阶段不再自动运行文档、后端、前端、Secret Scan 或 Relay Review。合并人必须自行完成差异、身份、测试、密钥、风险和回滚核对；维护者 merge 即代表本仓库流程中的人工复核完成，不额外要求 approval。

合并后的 `sync-original-cloud.yml` 仍会读取 PR/直接维护提交身份，执行完整快进边界、成员 Token 映射和每次 push 后 SHA 核对；停用 PR 阶段 CI 不会取消同步阶段的身份安全检查。

## 4. GitHub 合并后自动同步

工作流 [`.github/workflows/sync-original-cloud.yml`](.github/workflows/sync-original-cloud.yml) 监听 GitHub `master` 的推送，流程如下：

当前同步 job 使用专用 `self-hosted/linux/hct-sync` Runner，并在 job 条件中限制为
GitHub `master`。该 Runner 需要由仓库 Owner 在 GitHub `Settings → Actions → Runners`
中注册并保持在线；未注册或离线时同步会等待/失败，不应改用未知 Runner 或人工向内部
仓库推送。Billing/付款限制需要由 Owner 在 `Settings → Billing & licensing` 处理，
代码不能替代账户账单恢复。

1. PR 合并进入 GitHub `master`。
2. Actions 使用维护者凭据只读获取内部 `master`，由
   [同步路径规划器](.github/scripts/cloud_sync_plan.py)计算第一父链快进节点；内部 SHA 不在
   GitHub 第一父链时直接停止。
3. 普通合并提交必须关联唯一的、已合并到 `master` 的 PR；工作流读取 PR 作者和 PR 内全部提交账号。
4. 仅有一父提交且先查不到唯一已合并 master PR 的直接维护提交允许走安全例外：[身份解析脚本](.github/scripts/cloud_sync_identity.py)要求 GitHub `author.login` 与 `committer.login` 均存在、完全一致且属于已登记成员，`parents` 必须明确为单父。工作流先用完整历史检出的本地 Git diff 生成未截断文件清单，再要求状态只能是 `added/modified`、不得有 `previous_filename`，并逐段校验规范化路径；所有变更只能位于 `doc/` 或 `docs/`。会议记录等维护资料可因此按真实作者 Token 同步；squash/rebase 等虽为单父但能关联 PR 的提交必须走 PR 作者和边界校验；源代码、配置、工作流和未知身份的直接提交仍然在任何 push 前失败。
5. 对 PR 路径，[身份解析脚本](.github/scripts/cloud_sync_identity.py)确认 PR 作者与全部 PR 提交账号属于同一成员，再返回该成员的内部用户名和 Token Secret 名称，不排除或跳过混合作者提交。
6. 每次 push 还要计算上一内部 SHA 到目标 SHA 的全部新增可达提交；只允许已核验的 PR 边界或已核验的直接维护提交。出现其他 PR、未授权直接提交、混合作者或缺少 Secret 时，在任何 push 前失败。
7. 工作流在任何 push 前预检整批待同步节点；未知作者、author/committer 不一致、混合提交账号、跨 PR 新增提交或缺少 Secret 时直接失败，不使用其他成员 Token。PR 阶段的 `Sync identity preflight` 会提前执行 PR 身份核对，直接维护提交则由合并后的同步工作流按上述白名单核对。
8. 每次 push 后立即核对内部 SHA，最终两端 SHA 必须一致；历史分叉时失败并停止，不强制覆盖。

管理员需要在 GitHub `Settings → Secrets and variables → Actions` 中配置：

- `CLOUD_REPO_USERNAME`：GitHub 用户 `Meyt1n` 对应的内部云端用户名，同时用于读取内部 `master`；
- `CLOUD_REPO_PASSWORD`：GitHub 用户 `Meyt1n` 对应的内部云端 Token，同时用于只读获取内部 `master`；
- `CLOUD_TOKEN_SHEN_HUANG_123`：GitHub 用户 `Shen-huang-123` 对应的内部云端 Token，Token 所属统计账号为 `zhang`；
- `CLOUD_TOKEN_RY12_20`：GitHub 用户 `ry12-20` 对应的内部云端 Token，Token 所属统计账号为 `ry12-20`；
- `CLOUD_TOKEN_389883656_LGTM`：GitHub 用户 `389883656-lgtm` 对应的内部云端 Token，Token 所属统计账号为 `yanghuan`；
- `CLOUD_TOKEN_JIN_123_ZIP`：GitHub 用户 `jin-123-zip` 对应的内部云端 Token，Token 所属统计账号为 `jin-123-zip`。

当前映射如下：

| GitHub 成员（PR 作者或直接维护提交作者） | 允许的提交身份 | 内部账号 | Token Secret |
|---|---|---|---|
| `Meyt1n` | GitHub `Meyt1n`，或已登记的 Meyt1n Git 姓名/邮箱 | `CLOUD_REPO_USERNAME` | `CLOUD_REPO_PASSWORD` |
| `Shen-huang-123` | GitHub `Shen-huang-123`，或 `zhang <z85963541@qq.com>` | `zhang` | `CLOUD_TOKEN_SHEN_HUANG_123` |
| `ry12-20` | 必须由 GitHub 识别为 `ry12-20`；登记邮箱前不接受未关联提交 | `ry12-20` | `CLOUD_TOKEN_RY12_20` |
| `389883656-lgtm` | GitHub `389883656-lgtm`，或 `389883656-lgtm/yanghuan <389883656@qq.com>` | `yanghuan` | `CLOUD_TOKEN_389883656_LGTM` |
| `jin-123-zip` | GitHub `jin-123-zip`，或 `jin-123-zip <3487355487@qq.com>` | `jin-123-zip` | `CLOUD_TOKEN_JIN_123_ZIP` |

新增成员时，按 `CLOUD_TOKEN_<规范化 GitHub 用户名>` 建立独立 Repository Secret，并同步更新 `.github/scripts/cloud_sync_identity.py` 的 GitHub 登录名、Git 姓名/邮箱、内部用户名和单元测试。Secrets 只能保存在 GitHub Repository secrets 中，不能写入代码、PR、日志、远程 URL 或聊天消息。成员不需要把内部云端凭据安装到个人电脑或发给 agent。

一个 PR 只能归属一个 Token。若 PR 中包含多名成员的提交，合并前应拆分 PR；Git 协议无法让同一次 master push 同时归属多个 Token。

### 4.1 Artifact 策略

API/Browser CI、历史同步执行和 dry-run 计划当前均不调用 `actions/upload-artifact`，
因此不会产生新的 Artifact 存储占用。测试结论、同步前预检、每次 push 后 SHA 核对和最终
双端 SHA 以 Actions 日志为准；历史已存在的 Artifact 需要由管理员在 GitHub Actions
页面单独删除。不得为了清理存储而删除同步日志或改写仓库历史。

## 5. 同步验收

PR 合并后，在 Actions 页面确认“同步 master 到原云端仓库”运行成功，然后核对两个远端的 `master`：

```bash
git ls-remote https://github.com/Meyt1n/issedu_ysu2026_3709.git refs/heads/master
git ls-remote http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git refs/heads/master
```

两条命令输出的提交 SHA 必须完全相同。若只有一个仓库更新，不要重新创建提交，也不要手动双推；先查看 Actions 日志和远端权限，再由管理员重新运行同步工作流。

同步后，内部云端看到的是 GitHub 合并后的同一份提交历史。成员的姓名、邮箱和提交作者信息保持原样；统计系统如果只按 Token 归属，则一个 PR 的合并 push 会归到该 PR 成员对应的内部账号。Actions 日志应显示 PR 编号、GitHub 作者、提交数量和 Secret 名称，但不得显示 Token 值。

PR24、PR28 等已经使用旧凭据完成的历史记录不能靠重新 push 同一 SHA 改写；成员映射只从本工作流合并后的新同步开始生效。

### 5.1 一次性历史归属修复

对于已经合并的旧 PR，如果内部 `master` 是目标 merge commit 的祖先，但某个 PR 分支提交来自更早的共同祖先，直接把该分支提交推到内部 `master` 会被 Git 拒绝为 non-fast-forward。一次性工作流 `execute-cloud-history-sync.yml` 会把这类动作标记为 `force-with-lease-staging`：只允许 PR 分支提交使用精确旧 SHA 的 `--force-with-lease` 暂存，随后用对应 PR merge Token 快进到 merge commit，再继续普通快进。

该模式只用于已审计的历史修复，不允许裸 `--force`，不允许普通提交或 merge commit使用租约暂存；每次操作前后都校验内部 SHA，最终必须与 GitHub `master` 完全一致。若旧 SHA 变化、内部存在未审计独立提交或目标不是已确认的 PR 分支提交，工作流必须停止。

## 6. 两端分叉时的处理

普通成员发现分叉只报告，不处理。管理员先暂停同步，保存 GitHub SHA、内部云端 SHA、分叉原因和平台统计快照。内部独立提交需要保留时，必须单独评审历史拓扑与 Token 归属；自动工作流不得沿非第一父祖先路径推送，因为一个目标可能同时引入多个成员提交。只有确认内部云端没有需要保留的独立提交且统计影响已获负责人批准时，才可以使用带旧 SHA 保护的 `--force-with-lease`。禁止使用裸 `--force`：

```bash
git ls-remote https://github.com/Meyt1n/issedu_ysu2026_3709.git refs/heads/master
git ls-remote http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git refs/heads/master
git fetch github master
git tag backup/cloud-master-before-recovery-<日期> <旧内部云端SHA>
git push --force-with-lease=refs/heads/master:<旧内部云端SHA> http://119.3.217.118:30181/29092881243490627/issedu_ysu2026_3709.git <GitHub权威SHA>:refs/heads/master
```

恢复后必须再次核对两端 SHA、Git author 和平台 Token 归属。如果内部云端存在应保留的独立提交，
先备份并由负责人制定逐成员处置方案，不能直接覆盖或交给自动 workflow。恢复同步必须证明：

- GitHub 与内部提交具有已确认的共同基线；
- 内部 SHA 位于 GitHub `master` 第一父链；
- 每个同步节点只引入关联 PR 的提交及该 PR merge commit；
- PR 作者与全部 PR 提交身份一致，不跳过任何提交；
- Actions 完成后两个远端的 `master` SHA 完全一致。

### 6.1 2026-08-13 归属事故

PR #136 的祖先路径同步先后使用 `zhang` Token 推送 `e38568b → 34a2bd1` 和
`34a2bd1 → c2507c9`。前者额外引入 `jin-123-zip` 提交，后者额外引入 `ry12-20`
的 PR #135，导致内部平台新记录错误归入 `zhang`。Git author 未改变。

事故发现后 workflow 已禁用。平台负责人应按 SHA 更正 `b4ab7a3` 为 `jin-123-zip`，
更正 `9c2f62b`、`2a797ec`、`5acfa1d` 为 `ry12-20`；不得用重复 push 代替平台记录修正。

## 7. 分支生命周期

- PR 合并后，维护者删除对应的 GitHub 远程分支；
- 已关闭且明确为一次性测试的分支，在确认没有待保留提交后可以删除；
- `master`、当前开发分支和仍有未合并 PR 的分支不得删除；
- 删除分支前先确认 PR 状态和分支 SHA，不用删除分支掩盖未完成任务。

GitHub CLI 示例：

```bash
gh pr view <PR编号> --repo Meyt1n/issedu_ysu2026_3709
gh api --method DELETE repos/Meyt1n/issedu_ysu2026_3709/git/refs/heads/<分支名>
```

## 8. 最终原则

GitHub 负责开发、Review 和合并；内部云端按 PR 成员 Token 自动接收 GitHub `master` 的同一提交。成员只需使用自己的 GitHub 分支和已登记 Git 提交身份完成单一作者 PR，不需要、也不允许再向两个仓库重复提交。
