# Web 用户端

计划使用 Vue 3、TypeScript 和 Vite，实现登录、家庭成员、药物识别、医疗问答、知识库和个人数据管理。

当前 `index.html` 与 `assets/` 是零构建依赖的本地 Demo，由 FastAPI 直接提供，用于验证页面、接口、安全升级和本地部署流程。正式 Vue 工程建立后应保留相同的接口与安全行为，并逐步替换该演示页面。

前端只负责展示和输入校验，权限判断、医疗安全分级与数据隔离必须在服务端执行。页面必须明确显示 AI 结果的不确定性、引用、模型版本和免责声明。
